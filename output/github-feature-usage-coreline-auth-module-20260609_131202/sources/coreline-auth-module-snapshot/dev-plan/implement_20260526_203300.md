# implement_20260526_203300.md

작성 일시: `2026-05-26 20:33:00 KST`

이 문서는 실제 SMTP/OAuth/Redis/Postgres/WebAuthn 외부 환경 검증은 제외하되, 그 전까지 로컬에서 구현 가능한 남은 운영화 항목을 모두 마무리하기 위한 작업 문서다.

## 개발 목적
- 외부 인프라 없이도 운영 readiness를 CLI/API/UI/문서에서 일관되게 확인한다.
- 실제 외부 환경이 준비되면 바로 검증할 수 있도록 configuration check와 선택적 연결 검증 entrypoint를 제공한다.
- release/RC 판단에 필요한 CHANGELOG, runbook, smoke command를 정리한다.

## 개발 범위
- `coreline_auth.ops_readiness` 모듈 추가.
- `python -m coreline_auth.ops_readiness` CLI와 `make readiness-check` 추가.
- demo `/system`이 공통 readiness 모듈을 사용하도록 정리.
- README, self-test, security checklist, CHANGELOG 업데이트.
- 회귀 테스트 추가 및 전체 pytest 통과.

## 제외 범위
- 실제 SMTP 메일 발송.
- 실제 Google/Facebook OAuth callback E2E.
- 실제 Redis/Postgres 서버를 필수로 띄우는 테스트.
- WebAuthn attestation object 전체 파싱/브라우저 ceremony 완전 구현.
- SaaS tenant/team/billing 확장.

## 참조 문서
- [README.md](../README.md)
- [docs/self-test-webapp.md](../docs/self-test-webapp.md)
- [docs/security-checklist.md](../docs/security-checklist.md)
- [dev-plan/implement_20260526_202038.md](./implement_20260526_202038.md)

## 공통 진행 규칙
- 외부 secret 값을 출력하지 않는다.
- 외부 연결 검증은 명시 옵션일 때만 수행한다.
- 기본 테스트는 로컬/인메모리/SQLite만으로 통과해야 한다.
- CoreMCP import를 추가하지 않는다.

## Phase 상태 요약
- [x] Phase 1 완료 — 공통 readiness 모듈/CLI
- [x] Phase 2 완료 — demo `/system` 통합
- [x] Phase 3 완료 — 문서/CHANGELOG/Makefile
- [x] Phase 4 완료 — 테스트/완료 보고

## Phase 1. 공통 readiness 모듈/CLI
### 구현 태스크
- [x] `ops_readiness.py` 추가.
- [x] secret-safe readiness dataclass와 env 검사 구현.
- [x] JSON/text CLI 출력 구현.

### 자체 테스트
- [x] env별 ready/missing 판정 테스트.
- [x] CLI 출력에 secret 미노출 테스트.

## Phase 2. demo `/system` 통합
### 구현 태스크
- [x] `/system` readiness table이 공통 모듈을 사용하도록 변경.
- [x] WebAuthn/SMTP/OAuth/Redis/Postgres 상태 설명 정리.

### 자체 테스트
- [x] `/system` 화면 회귀 테스트.
- [x] readiness secret 미노출 테스트.

## Phase 3. 문서/CHANGELOG/Makefile
### 구현 태스크
- [x] `make readiness-check` 추가.
- [x] README/self-test/security checklist 업데이트.
- [x] CHANGELOG에 rc2 후보 내용 추가.

### 자체 테스트
- [x] `make readiness-check` 실행.

## Phase 4. 테스트/완료 보고
### 자체 테스트
- [x] `uv run pytest tests/test_ops_readiness.py -q` 통과.
- [x] `uv run pytest tests/test_demo_webapp.py -q` 통과.
- [x] `uv run pytest -q` 전체 통과.
- [x] import guard 통과.


## 최종 결과 요약
- `coreline_auth.ops_readiness`를 추가해 Google/Facebook OAuth, SMTP, Redis, Postgres, WebAuthn readiness를 secret-safe 방식으로 검사한다.
- `make readiness-check`와 `python -m coreline_auth.ops_readiness --json`으로 CLI/CI에서 동일한 검사를 수행한다.
- `/system`은 공통 readiness 모듈을 사용해 UI와 CLI 판정이 어긋나지 않게 했다.
- README, self-test, security checklist, CHANGELOG에 rc2 후보 운영화 내용을 반영했다.
- 검증: `tests/test_ops_readiness.py` 3 passed, `tests/test_demo_webapp.py` 24 passed, 전체 `uv run pytest -q` 147 passed / 1 skipped, import guard 통과.

## 잔여 리스크 / 후속 과제
- 실제 SMTP 발송, 실제 Google/Facebook OAuth callback, 실제 Redis/Postgres 접속, WebAuthn 브라우저 ceremony는 외부 환경 준비 후 실행 검증한다.
