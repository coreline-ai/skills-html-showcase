# implement_20260618_221706.md

작성 일시: `2026-06-18 22:17:06 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

demo17 산출물 눈검수에서 드러난 **"validate/quality/completion은 통과했지만 사용자가 보는 화면에 남는 시각 결함" 3종(G1~G3)** 을 `skills/adaptive-html-final` **본체 차원에서 재발 방지**한다. 산출물(output)은 이미 수정·커밋(`b6a1f99`)했으며, 이 계획은 **향후 생성물에서 같은 결함이 안 나오게** 하는 것이 목표다.

| Code | 결함 | 근본 원인 | 영향 파일 | 정적 검출 가능? |
|---|---|---|---|---|
| G1 | 좁은 표 열의 짧은 코드 줄바꿈 (`100`→`10`+`0`) | `components.css`의 `code{}`(L88)에 `white-space:nowrap` 없음 → 좁은 열에서 3자리 코드가 2줄로 깨짐 | `components.css`(코어) / 작성 규칙 | △(정적 가드 가능하나 오탐 주의) |
| G2 | wg-04 결정트리 노드 겹침 (8·13페이지) | wg-04 **SVG 노드 좌표가 생성 시 겹치게 찍힘**. 위젯 CSS는 정상, 좌표(콘텐츠)가 문제 | 생성 절차 / 렌더 감사 | ✕(정적 불가 → Playwright 필요) |
| G3 | 중첩 흰 카드 안 링크 저대비 (1.65:1) | `.try a`(L179)가 다크 전용 `--link-on-dark`(#ffb6c0)를 흰 inner 카드(`.summary-card`/`.box`/…) 링크에까지 상속, reset 누락 | `components.css`(코어) | △(렌더 대비 감사로 확실) |

## 릴리스 전략 — 누적 일괄 버전 승격 (확정)

> 사용자 방침(2026-06-18): **수정 1건마다 버전 bump하지 않는다.** 코어 해시 리베이스·examples 18종 재인라인·버전 표면 동기화 비용이 크므로, **G1~G3 + 이후 누적될 스킬 하드닝 항목을 모아 한 번의 릴리스(목표 `5.10.6`)로 일괄 승격**한다.

- **지금은 구현·버전 bump를 하지 않는다.** 이 문서는 계획 + 누적 백로그(§다음 할 일)만 유지한다.
- 누적 기간 동안 산출물에서 새 결함이 발견되면 (a) **output 측 즉시 수정**(이미 `b6a1f99`처럼)으로 사용자 영향을 막고, (b) **스킬 측 근본 수정은 본 백로그에 항목으로 추가**해 일괄 릴리스에 포함한다.
- **batch 트리거**(아래 중 하나): 백로그가 충분히 쌓였다고 사용자가 판단 / 사용자가 명시적으로 "릴리스 진행" 지시 / 코어 변경이 불가피한 항목이 누적됨. 트리거 시 D1·D2 결정 → Phase 0~4 일괄 실행 → `release-approval-v5.10.6.md`.

## 개발 범위

- **G1·G3 CSS 하드닝**: 좁은 표 셀 코드 nowrap, `.try` 내부 흰 카드 링크색 reset을 정본 자산에 반영(반영 위치는 §선행 결정 게이트 D1).
- **G2 렌더 감사 게이트 신설**: Playwright 기반 **노드 박스 overlap 감사**(diagram 노드)를 `render-audit.json` 계약 + `completion_check.py`에 추가. 중첩 링크/저대비도 같은 감사 패스에서 검출(G3 보강).
- **작성 프로토콜 보강**: `docs/adaptive-html-final-template-authoring-protocol.md`에 G1~G3 "실패 예시 → 올바른 처리"(상태 코드는 `.status-pill`/nowrap, wg-04 노드 간격 규칙, 중첩 카드 링크는 `--ink`/`--accent-2`) 추가.
- 변경 시 examples 18종 재인라인·`css-integrity.json`·source snapshot·`.skill` 재패키징 동기화(코어 해시 변동 시) + 버전/governance 정합(D2).

## 제외 범위

- **output/ 산출물 변경**(이미 `b6a1f99`로 완료) 및 output을 스킬 입력/정본으로 승격하는 것.
- 승인 없는 버전 bump(D2 결정 전 임의 5.10.6 금지). 버전 변경은 사용자 명시 승인 또는 `dev-plan/release-approval-v5.10.6.md` 필요.
- 이미 강제된 게이트 재구현(중복 신설 금지). 기존 다크 대비 감사·표 overflow 가드와 중복하지 않음을 확인 후에만 신설.
- wg-04 위젯 CSS 재설계, SVG 자동 레이아웃 엔진 도입(좌표 자동계산) 같은 대규모 변경.
- 코어 디자인 토큰/8테마 시스템 개편.

## 참조 문서

- [AGENTS.md](../AGENTS.md) — §2.1 버전 릴리스 정합 체크리스트
- [docs/adaptive-html-final-template-authoring-protocol.md](../docs/adaptive-html-final-template-authoring-protocol.md) — 작성 프로토콜(M계열)
- [dev-plan/implement_20260614_174756.md](./implement_20260614_174756.md) — 이전 M계열 마이크로 레이아웃 계획(동일 결 작업)
- [dev-plan/release-approval-v5.10.4.md](./release-approval-v5.10.4.md) · [release-approval-v5.10.5.md](./release-approval-v5.10.5.md) — 릴리스 승인 형식
- 산출물 수정 증거(이 계획의 입력 아님, 참고용): 커밋 `b6a1f99` (demo17 index·08·13 정정)
- 검증기: `scripts/validate_output.py` · `scripts/quality_contract_check.py` · `scripts/completion_check.py` · `tests/test_governance_gates.py`

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 신규 게이트는 **악성 fixture를 먼저 만들어 실제로 실패**하는지 확인한 뒤 반영한다.
- 버전·governance_count는 `skills/adaptive-html-final/manifest.json` 단일 출처와 동기화한다.
- output은 독립 결과물이다 — 검증·정본 입력으로 쓰지 않는다.

## 현재 상태 (단일 출처 실측, 2026-06-18)

| 항목 | 값 |
|---|---|
| manifest.version | `5.10.5` |
| manifest.quality.governance_count | `162` |
| test_governance_gates.py 실측 | `162/162` |
| 코어-해시 5종 | `theme.css · components.css · visual-components.css · layouts.css · print.css` |
| G1/G3 대상 | `components.css` (코어) — `code{}`(L88), `.try a`(L179) |

## 선행 결정 게이트 (CSS/버전 착수 전 필수)

| 결정 | 선택지 | 영향 |
|---|---|---|
| **D1. CSS 반영 위치** | (a) 코어 `components.css` 직접 수정 → **core-css-sha256 리베이스 + examples 18종 재인라인** / (b) 조건부 자산(`editorial-patterns.css`, inline 순서상 components 뒤 → 캐스케이드로 override 가능)에 reset/override만 추가 → **코어 해시 불변** | (a) 파급 큼·정본 일원화 / (b) 파급 작음·override 레이어 분산 |
| **D2. 버전 정책 (확정: 누적 일괄)** | 개별 수정마다 bump하지 않고 **누적 후 한 번에 `5.10.6` 일괄 승격**(§릴리스 전략). batch 트리거 전까지 코어 CSS 미변경·버전 `5.10.5` 유지 | 버전/해시 churn 최소화 |

> **정합 제약:** batch 릴리스 시 D1-a(코어 변경)면 5.10.6 승격 + examples 재인라인 필수. D1-b(조건부 override)면 코어 해시 불변이나, **게이트 추가로 governance_count는 어느 경우든 증가**하므로 manifest·README 표면 동기화는 공통 필수. **트리거 전에는 D1 미확정·코어 미변경.**

## Phase 상태 요약

- [ ] Phase 0 완료 — G1~G3 재현 fixture (현행이 통과=결함 입증)
- [ ] Phase 1 완료 — G1·G3 CSS 하드닝 (D1 결정 반영)
- [ ] Phase 2 완료 — G2 Playwright 노드 overlap + 중첩 링크 대비 감사 게이트 신설
- [ ] Phase 3 완료 — examples 재인라인·해시·snapshot·.skill 동기화 (코어 변경 시)
- [ ] Phase 4 완료 — 작성 프로토콜 보강 + 버전·governance 정합 (D2)

---

## Phase 0. G1~G3 재현 fixture

### 목표
- 세 결함이 **현행 검증을 통과**(=결함이 안 잡힘)함을 fixture로 입증해 회귀 기준선을 만든다.

### 구현 태스크
- [ ] G1: 좁은 표 열에 `<td><code>100</code>`를 둔 HTML이 현행 `validate/quality`를 통과함을 보인다.
- [ ] G3: `.try > .summary-card > a`(흰 카드 안 링크)가 현행 게이트를 통과(저대비 미검출)함을 보인다.
- [ ] G2: wg-04 노드 2개가 좌표상 겹치는 SVG가 현행 정적 게이트를 통과함을 보인다(정적으로는 불가 확인).
- [ ] 재현 fixture를 `tests/` 보조 자산 또는 dev-plan 첨부로 보관(회귀 비교용).

### 자체 테스트
- [ ] 세 fixture 모두 "현행은 통과(=결함)"가 재현된다.
- [ ] 기존 examples는 그대로 통과(무회귀 기준선 기록).

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 1. G1·G3 CSS 하드닝 (D1 결정 반영)

### 목표
- 좁은 표 셀 코드 줄바꿈(G1)과 `.try` 내부 흰 카드 링크 저대비(G3)를 정본 CSS로 닫는다.

### 구현 태스크
- [ ] **D1·D2 결정 확정 기록**(미결이면 Phase 1 착수 보류).
- [ ] G3: `.try :is(.box,.summary-card,.cta-box,.card-block,.mini-card) a`에 `color:var(--accent-2)`(또는 `--ink`)+밑줄 reset 추가. (다크 테마는 inner 카드가 어두워지므로 `--ink`/`--accent-2` 토큰이 자동 적응함을 확인)
- [ ] G1: 표 셀 안 짧은 코드 줄바꿈 방지. **오탐 주의** — 긴 인라인 코드(`Expect: 100-continue`)는 `.tbl`/`.table-scroll` overflow에 맡기고, 상태코드형 짧은 코드만 nowrap 되도록 범위를 좁힌다(예: 첫 열 `td:first-child code` 또는 `.status-pill` 정본 권장).
- [ ] 반영 위치는 D1 결정에 따름: (a) `components.css` 직접 / (b) `editorial-patterns.css` override.
- [ ] 코어(`components.css`) 변경 시 `code{}`·`.try a` 주변 회귀(인라인 코드·`.try` 다크 링크 정상 동작) 확인.

### 자체 테스트
- [ ] Phase 0의 G1·G3 fixture가 이제 **실패(=검출/정상화)** 한다.
- [ ] 기존 examples 18종 회귀 0 — `validate`/`quality` OK.
- [ ] `.try` 직속 링크(다크 배경)는 여전히 `--link-on-dark`로 보인다(reset이 inner 카드만 한정).
- [ ] 8테마 전부에서 inner 카드 링크 대비 ≥ 4.5:1.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 2. G2 Playwright 마이크로 레이아웃 감사 게이트

### 목표
- 정적으로 못 잡는 **diagram 노드 박스 overlap**(G2)과 **중첩 링크/텍스트 저대비**(G3 보강)를 렌더 감사로 검출한다. `render-audit.json` 계약 + `completion_check.py`에 흡수(Playwright 직접 구동은 별도 캡쳐 차수, 검증기는 아티팩트만 검사 — 기존 render-audit 방식과 동일).

### 구현 태스크
- [ ] 노드 overlap 측정 규칙 정의: `.wg-04-node`(및 vt diagram 노드)의 box overlap이 임계(>4px×4px)면 실패. 측정값을 `render-audit.json`에 기록.
- [ ] 저대비 측정 규칙 정의: 보이는 텍스트/링크 노드 대비 < 4.5:1(대형 3:1)이면 실패. 기존 다크 대비 감사와 **중복 회피**(중첩 inner-card 컨텍스트만 신규 커버).
- [ ] `completion_check.py`가 `render-audit.json`의 `node_overlap_ok`·`contrast_ok` 필드를 검사하도록 추가(아티팩트 부재/false면 실패).
- [ ] 외부 캡쳐용 audit 스크립트(예: `scripts/micro_layout_audit.mjs`)를 분리 — 검증기는 구동하지 않고 결과 JSON만 읽는다.
- [ ] 악성 fixture(겹치는 노드 SVG / 저대비 링크)로 게이트 실패 확인.

### 자체 테스트
- [ ] 겹침 fixture·저대비 fixture가 게이트에서 실패한다.
- [ ] demo17 17페이지(수정본)·examples는 `node_overlap_ok=true`·`contrast_ok=true`.
- [ ] 기존 render-audit(overflow·screenshot) 계약 회귀 0.
- [ ] governance 자체 테스트에 신규 게이트 catch/pass 추가, manifest count와 일치.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 3. examples 재인라인·해시·snapshot·.skill 동기화

### 목표
- 코어 CSS가 변경된 경우(D1-a) 해시/스냅샷/패키지를 한 세트로 정합한다. (D1-b면 조건부 해시만 갱신)

### 구현 태스크
- [ ] core-css-sha256 재계산(코어 변경 시) + examples 18종 인라인 코어 CSS·해시 마커 재주입.
- [ ] `examples/sources/css-integrity.json`(core/asset/conditional 해시) 재생성.
- [ ] `examples/sources/assets/*.css` 변경 자산 동기화.
- [ ] `examples/sources/adaptive-html-final-manifest.json` = 현행 manifest와 byte-equal.
- [ ] `skills/adaptive-html-final.skill` 재패키징(캐시류 제외, 워킹트리 byte-match).

### 자체 테스트
- [ ] `validate_output.py examples --skill-dir` OK (inline hash·verbatim·snapshot·.skill byte-match 전부).
- [ ] 코어 미변경(D1-b) 시 core-css-sha256 불변 확인.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 4. 작성 프로토콜 보강 + 버전·governance 정합

### 목표
- G1~G3 재발 방지 규칙을 문서화하고, 버전/거버넌스 표면을 단일 출처로 동기화한다.

### 구현 태스크
- [ ] `docs/adaptive-html-final-template-authoring-protocol.md`에 G1~G3 "실패→정답" 추가(상태코드=`.status-pill`/nowrap, wg-04 노드 최소 간격, 중첩 카드 링크=`--ink`/`--accent-2`).
- [ ] D2 결정에 따라: (a) `dev-plan/release-approval-v5.10.6.md` 작성 + manifest/SKILL/CHANGELOG/README/AGENTS/Guide/examples meta·footer/source snapshot 버전 표면 일괄 5.10.6 / (b) 5.10.5 유지 시 governance_count만 갱신.
- [ ] `manifest.quality.governance_count` + README 게이트 현황/표/트리 수치 동기화(신규 게이트 반영).
- [ ] AGENTS §2.1 버전 표면 스캔으로 잔여 구버전 표기 0 확인.

### 자체 테스트
- [ ] `test_governance_gates.py` = 신규 count 통과, manifest와 일치.
- [ ] `validate_output.py examples` OK(버전 표면·README 표면 게이트 포함).
- [ ] `quality_contract_check.py`·`completion_check.py` OK.
- [ ] README badge/NOTE/표/트리 governance 수치 = manifest count.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 전체 워크스트림 완료

---

## 다음 할 일 (누적 백로그 · 일괄 릴리스 대상)

batch 릴리스 전까지 **여기에 항목만 누적**한다. 코어 변경 필요 여부 = 버전 bump 트리거. (status: ⏳대기 / ▶진행 / ✅완료)

| ID | 항목 | 반영 위치(예상) | 코어 변경? | 출처 | status |
|---|---|---|---|---|---|
| G1 | 좁은 표 열 짧은 코드 nowrap | `components.css` 또는 `editorial-patterns.css` override / 작성 규칙 | 조건부 가능 | 본 계획 | ⏳ |
| G2 | wg-04 노드 overlap Playwright 감사 게이트 | `completion_check.py` + `render-audit.json` + audit 스크립트 | ✕(게이트만, governance++) | 본 계획 | ⏳ |
| G3 | `.try` 중첩 흰 카드 링크색 reset | `components.css`(코어) 또는 `editorial-patterns.css` override | 조건부 가능 | 본 계획 | ⏳ |
| B1 | 단색 rail unique-count 등 강화 micro-layout audit | audit 스크립트 + 게이트 | ✕(게이트) | [implement_20260614_174756](./implement_20260614_174756.md) 후속/선택 | ⏳ |
| B2 | 모드별 개별 `Mode Build Sheet`/`build-evidence.json` 분리 저장 | 생성 절차 문서 | ✕(절차) | 동상 | ⏳ |
| B3 | `docs/adaptive-html-final-17-mode-sequential-runbook.md` 신설 | docs | ✕(문서) | 동상 | ⏳ |

> 신규 결함 발견 시: ① output 측 즉시 수정(사용자 영향 차단) → ② 위 표에 스킬 측 항목 1줄 추가. 코어 변경 항목이 1개라도 확정되면 batch 릴리스를 `5.10.6`로 잡는다.

### 즉시 다음 할 일 (이번 차수 종료 시점 기준)

1. **대기**: 추가 demo17/산출물 눈검수에서 결함을 더 모은다(사용자 검수 주도). 발견 시 output 즉시 수정 + 백로그 항목 추가.
2. **트리거 시 착수 순서**: D1·D2 확정 → Phase 0(G1~G3 재현 fixture) → Phase 1~2(CSS·감사 게이트) → Phase 3(재인라인/해시) → Phase 4(`release-approval-v5.10.6.md` + 버전·governance 표면 일괄 정합).
3. **착수 트리거 대기 중에는** 코어 CSS·버전·`.skill`을 건드리지 않는다(현행 5.10.5/162 유지).

## 문서 기반 제약 사항

- 무 JS 불변(JSON-LD만). 감사 스크립트는 외부 캡쳐 차수에서 구동하고 출력 HTML에 JS를 넣지 않는다.
- §0.6 결정표·`MODE_TEMPLATE_CONTRACTS`·`widget-system.md` 3자 정합을 깨지 않는다(이번 변경은 위젯 계약 무관).
- 코어 CSS 변경 시 버전 표면 게이트(`output_visible_version_stale`·`readme_*`·`skill_package_*`)를 한 세트로 갱신.

## 잔여 리스크 / 후속 과제

- G1 일반화의 오탐: 표 셀의 긴 인라인 코드까지 nowrap되면 가로 overflow 유발 → 범위를 짧은 코드/첫 열/`.status-pill`로 좁히는 것이 관건.
- G2 감사의 비용: Playwright 캡쳐 차수 의존 → 신규 산출물·examples에만 요구하고 과거 output에 소급하지 않는다(기존 render-audit 정책과 동일).
- 더 강한 audit(단색 rail unique-count 등, 이전 계획의 후속 항목)은 본 계획 범위 밖 — 필요 시 별도 워크스트림.
