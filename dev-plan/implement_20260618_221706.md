# implement_20260618_221706.md

작성 일시: `2026-06-18 22:17:06 KST`
갱신 일시: `2026-06-21 KST` — **batch 트리거 발동 → 5.10.6 릴리스 마스터 플랜으로 승격**

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

> ### ⚑ 활성화 배너 (2026-06-21)
> 사용자 지시로 **batch 트리거가 발동**되었다. 이 문서는 더 이상 "대기 백로그"가 아니라 **`5.10.6` 일괄 릴리스의 마스터 플랜**이다.
> - **D1 확정 = 코어 직접 반영.** 이미 트리에 착지한 미승격 코어 변경(Unreleased, `layouts.css`)이 존재하므로 override 레이어보다 정합적이다.
> - **D2 확정 = `5.10.6` 일괄 승격.** per-fix bump 금지, 아래 범위를 한 번에 승격한다.
> - **범위 결정(2026-06-21):** `W1(G1~G8) + B1~B3 정합 + Unreleased 흡수 + 경량 W3`. **mode 18(strategy-os)은 `5.11.0` 별도 차수**([implement_20260620_201946.md](implement_20260620_201946.md)), theme-token 마이그레이션·AGENTS §2.2 생성비용 최적화 등 **대형 로드맵은 5.10.6 제외**.
> - **코드 변경·커밋은 사용자 명시 요청 시에만.** 본 갱신은 문서 정비 단계다.

## 개발 목적

demo17 및 신규 산출물 눈검수에서 드러난 **"validate/quality/completion은 통과했지만 사용자가 보는 화면에 남는 시각 결함" 누적 항목(G1~G8)** 을 `skills/adaptive-html-final` **본체 차원에서 재발 방지**한다. 산출물(output)은 즉시 수정해 사용자 영향을 막고, 이 계획은 **향후 생성물에서 같은 결함이 안 나오게** 하는 것이 목표다. 5.10.6 승격 차수에서는 여기에 **(a) 이미 트리에 착지한 미승격 코어 변경(Unreleased)의 버전 흡수**와 **(b) 경량 W3 항목(prefers-contrast 접근성 후속·render-audit 캡처 배선 문서화·repo hygiene)** 을 함께 닫는다.

| Code | 결함 | 근본 원인 | 영향 파일 | 정적 검출 가능? |
|---|---|---|---|---|
| G1 | 좁은 표 열의 짧은 코드 줄바꿈 (`100`→`10`+`0`) | `components.css`의 `code{}`(L88)에 `white-space:nowrap` 없음 → 좁은 열에서 3자리 코드가 2줄로 깨짐 | `components.css`(코어) / 작성 규칙 | △(정적 가드 가능하나 오탐 주의) |
| G2 | wg-04 결정트리 노드 겹침 (8·13페이지) | wg-04 **SVG 노드 좌표가 생성 시 겹치게 찍힘**. 위젯 CSS는 정상, 좌표(콘텐츠)가 문제 | 생성 절차 / 렌더 감사 | ✕(정적 불가 → Playwright 필요) |
| G3 | 중첩 흰 카드 안 링크 저대비 (1.65:1) | `.try a`(L179)가 다크 전용 `--link-on-dark`(#ffb6c0)를 흰 inner 카드(`.summary-card`/`.box`/…) 링크에까지 상속, reset 누락 | `components.css`(코어) | △(렌더 대비 감사로 확실) |
| G4 | `source-preserve` 원문 보존 뷰의 좌측 레일/본문 텍스트 gutter 부족 | `.source-preserve` 기본 구조가 `border-left`와 본문 텍스트 사이의 내부 gutter를 충분히 보장하지 않음. 개별 산출물에서 inline style로 보정됨 | `editorial-patterns.css` / `source-preserve` 마크업 규칙 / 작성 규칙 | ○(CSS·마크업 정적 가드 가능) |
| G5 | `mini-card` 첫 태그(`Skeptic · 회의주의자`)와 아래 제목/본문이 붙어 보임 | `.mini-card > .tag:first-child`와 후속 `h3/p` 사이의 vertical rhythm 정본 규칙 없음. 개별 산출물에서 inline style로 보정됨 | `components.css`(코어) 또는 조건부 override / 작성 규칙 | ○(CSS 정적 가드 가능) |
| G6 | 목차가 별도 섹션이 아니라 첫 본문 섹션 내부에 포함됨 | TOC 생성이 `executive_summary()` 같은 콘텐츠 블록 안에서 반환되어 `<section class="executive-summary">` 내부에 중첩됨 | 생성 절차 / layout placeholder / 작성 규칙 | ○(DOM 순서 정적 가드 가능) |
| G7 | 섹션 첫 타이틀이 상단 라인과 떨어져 보임 | `<h2>` 앞에 빈 anchor/div 등 선행 요소가 들어가 `section > h2:first-child` margin reset이 적용되지 않음 | 생성 마크업 / layout CSS 계약 / 작성 규칙 | ○(DOM 정적 가드 가능) |
| G8 | `core-insight` 상품 정의 카드의 상단 여백 과다 또는 빨간 그라데이션 소실 | 전역 `h3` margin이 `.core-insight` 내부에 누수되거나, 간격 보정을 위해 `core-insight--neutral`을 붙여 의도한 red gradient를 제거함 | `editorial-patterns.css` / `core-insight` 마크업 규칙 / 작성 규칙 | ○(CSS·DOM 정적 가드 가능) |

### 5.10.6 차수 추가 항목 (G1~G8 외)

| Code | 항목 | 근본 원인 / 현황 | 영향 파일 |
|---|---|---|---|
| **U0** | **Unreleased 코어 변경 버전 흡수** | 커밋 `87998ad`(manual_analysis 배지 padding)가 `layouts.css`(코어)를 수정 → 코어 해시 `7e151665`→`a73eb204`로 리베이스되어 examples 18종·css-integrity·`.skill`에 **이미 일관 반영**됐으나 **버전 미승격**(CHANGELOG `## Unreleased`에 잔류, manifest 버전은 5.10.5). 거버넌스 162/162는 통과(게이트가 버전 노트 hash를 비교하지 않음 → CI에 안 잡히는 표면 drift) | `CHANGELOG.md`(Unreleased) / `manifest.json` / `examples/sources/css-integrity.json`(`a73eb204`) |
| **A5** | **`prefers-contrast` 미구현** (v5.10.5 A1 forced-colors 후속) | `@media (forced-colors:active)`는 닫혔으나 `@media (prefers-contrast: more)` 규칙 0건 — 사용자 고대비 선호에 대한 단서 없음 | `theme.css`(코어) 또는 조건부 `theme-dark.css`/`widgets.css` (구현 시 결정) |
| **W3a** | **render-audit 캡처 배선 문서화** | `completion_check.py`·SKILL이 요구하는 `render-audit.json`의 유일 생산자 `scripts/render_audit_fulltest.mjs`가 repo 레벨(스킬 패키지 외부)에 있고 SKILL.md에 구체 실행 명령이 미기재 | `SKILL.md` / (선택)스킬 내 vendoring |
| **W3b** | **repo hygiene — untracked `build_*.py` 11개** | repo `scripts/`에 orginal_skill 출력 생성용 build 스크립트 11개가 untracked. 커밋/gitignore/이전 방침 미결 (스킬 본체 기능 아님, 리포 위생) | `.gitignore` / `scripts/` |
| **W3c** | **`manifest.json` `updated` 필드 lag** | `updated`가 `2026-06-15`로 v5.10.5 패키징(6/17)보다 뒤처짐. 비해시·비게이트 필드 — 5.10.6 승격 시 동기화 | `manifest.json` |

> **경량 W3 범위 한정:** prefers-contrast(A5)·render-audit 배선(W3a)·repo hygiene(W3b)·manifest updated(W3c)만 5.10.6에 포함한다. **output 보존 정책 문서화·visual-html 잔여 토큰화(wg-09 scroll-snap 등)·export 재검증 후속·theme-token 마이그레이션·AGENTS §2.2 생성비용 최적화는 5.10.6 제외**(별도 로드맵).

## 릴리스 전략 — 누적 일괄 버전 승격 (트리거 발동)

> 사용자 방침(2026-06-18): **수정 1건마다 버전 bump하지 않는다.** 코어 해시 리베이스·examples 18종 재인라인·버전 표면 동기화 비용이 크므로, **G1~G8 + 누적 스킬 하드닝 항목을 모아 한 번의 릴리스(`5.10.6`)로 일괄 승격**한다.
>
> **2026-06-21: batch 트리거 발동.** 더 이상 누적 대기하지 않고, 본 문서 범위(G1~G8 + U0 + 경량 W3)를 `5.10.6`으로 일괄 구현·승격한다. 착수 순서는 §Phase 상태 요약.

- 누적 기간 중 발견된 결함은 (a) **output 측 즉시 수정**으로 사용자 영향 차단, (b) **스킬 측 근본 수정은 본 계획에 항목으로 편입**. 현재 편입 완료(G1~G8 + U0 + A5 + W3a~c).
- **mode 18(strategy-os)은 본 차수에 합류시키지 않는다.** 신규 모드+위젯 8종은 patch(5.10.6)가 아니라 minor(`5.11.0`)로 별도 승격 — 릴리스 표면 과중·검증량 급증 회피.

## 개발 범위

- **G1·G3~G8 CSS/패턴/생성계약 하드닝**: 좁은 표 셀 코드 nowrap, `.try` 내부 흰 카드 링크색 reset, `source-preserve` 좌측 gutter, `mini-card` 첫 태그와 제목/본문 vertical rhythm, TOC 독립 섹션, `<h2>` anchor 위치, `core-insight` 내부 제목 간격·red gradient 보존을 **정본 코어 자산(D1=코어 직접)** 및 작성 규칙에 반영.
- **G2 렌더 감사 게이트 신설**: Playwright 기반 **노드 박스 overlap 감사**(diagram 노드)를 `render-audit.json` 계약 + `completion_check.py`에 추가. 중첩 링크/저대비도 같은 감사 패스에서 검출(G3 보강).
- **U0 Unreleased 버전 흡수**: 이미 반영된 `layouts.css` 배지 변경(`a73eb204`)을 `5.10.6` 정식 릴리스 노트로 승격. (v5.10.5 역사 노트는 보존, 신규 5.10.6 엔트리 추가)
- **A5 `prefers-contrast` 접근성 후속**: `@media (prefers-contrast: more)` 단서 추가(forced-colors와 동형 접근).
- **W3a 캡처 배선 문서화**: `render_audit_fulltest.mjs` 실행 명령을 SKILL.md/docs에 명시(필요 시 스킬 내 vendoring).
- **W3b repo hygiene**: untracked `build_*.py` 처리 방침 결정·반영. **W3c**: `manifest.json` `updated` 동기화.
- **작성 프로토콜 보강**: `docs/adaptive-html-final-template-authoring-protocol.md`에 G1~G8 "실패 예시 → 올바른 처리" 추가.
- 변경 시 examples 18종 재인라인·`css-integrity.json`·source snapshot·`.skill` 재패키징 동기화(코어 해시 변동 필연) + 버전/governance 정합.

## 제외 범위

- **mode 18(strategy-os) 신설** — `5.11.0` 별도 차수([implement_20260620_201946.md](implement_20260620_201946.md)).
- **대형 로드맵** — theme-token 마이그레이션([docs/theme-token-strategy.md](../docs/theme-token-strategy.md)), AGENTS §2.2 생성비용 최적화, export 재검증 후속([implement_20260606_072804.md](./implement_20260606_072804.md)), output 보존 정책, visual-html 잔여 토큰화(wg-09 scroll-snap/wg-13/wg-20 등). 5.10.6 제외.
- **output/ 산출물 변경** 및 output을 스킬 입력/정본으로 승격하는 것.
- 이미 강제된 게이트 재구현(중복 신설 금지). 기존 다크 대비 감사·표 overflow 가드와 중복하지 않음을 확인 후에만 신설.
- wg-04 위젯 CSS 재설계, SVG 자동 레이아웃 엔진 도입 같은 대규모 변경.
- 코어 디자인 토큰/8테마 시스템 개편.

## 참조 문서

- [AGENTS.md](../AGENTS.md) — §2.1 버전 릴리스 정합 체크리스트, §2.2 성능(범위 밖)
- [docs/adaptive-html-final-template-authoring-protocol.md](../docs/adaptive-html-final-template-authoring-protocol.md) — 작성 프로토콜(M계열)
- [dev-plan/implement_20260620_201946.md](./implement_20260620_201946.md) — **5.11.0 후속(mode 18)** 계획
- [dev-plan/implement_20260612_221316.md](./implement_20260612_221316.md) — A5/output 정책/visual-html 잔여 출처(일부 5.10.6, 나머지 로드맵)
- [dev-plan/implement_20260614_174756.md](./implement_20260614_174756.md) — 이전 M계열 마이크로 레이아웃 계획(B1/B2 출처)
- [dev-plan/release-approval-v5.10.4.md](./release-approval-v5.10.4.md) · [release-approval-v5.10.5.md](./release-approval-v5.10.5.md) — 릴리스 승인 형식
- 검증기: `scripts/validate_output.py` · `scripts/quality_contract_check.py` · `scripts/completion_check.py` · `tests/test_governance_gates.py`

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 신규 게이트는 **악성 fixture를 먼저 만들어 실제로 실패**하는지 확인한 뒤 반영한다.
- 버전·governance_count는 `skills/adaptive-html-final/manifest.json` 단일 출처와 동기화한다.
- output은 독립 결과물이다 — 검증·정본 입력으로 쓰지 않는다.
- **코드 변경·커밋은 사용자 명시 요청 시에만** 한다(현 워크스트림 규칙).

## 현재 상태 (단일 출처 실측, 2026-06-21)

| 항목 | 값 |
|---|---|
| manifest.version | `5.10.5` (→ 5.10.6 승격 대상) |
| manifest.quality.governance_count | `162` (신규 게이트로 증가 예정) |
| test_governance_gates.py 실측 | `162/162` |
| 코어-해시 5종 | `theme.css · components.css · visual-components.css · layouts.css · print.css` |
| **현 코어 해시(실측)** | **`a73eb204`** — Unreleased 배지 변경 반영본(css-integrity·examples 18종·`.skill` 일관). manifest 버전 노트는 여전히 `7e151665`(v5.10.5 역사 기록, 편집 금지) |
| G1/G3 대상 | `components.css` (코어) — `code{}`(L88), `.try a`(L179) |
| G4/G8 대상 | `editorial-patterns.css` (조건부) |
| U0 상태 | 코어 변경 **착지 완료**, **버전 미승격** — 5.10.6에 흡수 |

## 선행 결정 게이트 (확정 완료)

| 결정 | 상태 | 내용 |
|---|---|---|
| **D1. CSS 반영 위치** | ✅ **확정 = 코어 직접** | `components.css`/`editorial-patterns.css` 등 정본 자산에 직접 반영. **근거:** U0(layouts.css 코어 변경)이 이미 트리에 착지해 코어 해시가 어차피 리베이스되므로, override 레이어 분산보다 정본 일원화가 정합적. examples 18종 재인라인은 어느 경우든 필요. |
| **D2. 버전 정책** | ✅ **확정 = 5.10.6 일괄 승격** | 개별 bump 금지, 본 문서 범위(G1~G8+U0+경량 W3)를 한 번에 `5.10.6`(patch)으로 승격. mode 18은 `5.11.0`(minor) 별도. |

> **정합 제약:** D1=코어 직접이므로 5.10.6 승격 + examples 재인라인 필수. 게이트 추가로 `governance_count`도 증가하므로 manifest·README 등 버전/거버넌스 표면 동기화는 공통 필수.

## Phase 상태 요약

- [x] Phase 0 — G1~G8 재현 fixture (현행이 통과=결함 입증) + 무회귀 baseline **✅ 2026-06-21 (8/8 INVISIBLE, exit 0)**
- [x] Phase 1 — G1·G3~G8 CSS/패턴/생성계약 하드닝 + A5 prefers-contrast (D1=코어 직접) **✅ 2026-06-21 (G3 8테마 min 6.09:1; 거버넌스 161/162 = .skill staleness만, Phase 3 해소)**
- [x] Phase 2 — G2 Playwright 노드 overlap + 중첩 링크 대비 감사 게이트 + G3~G8 정적 가드 **✅ 2026-06-21 (14 게이트 체크, governance 176; G6/G7 examples 18/18 통과; .mjs가 G2/G3 실측 검출)**
- [x] Phase 3 — examples 18종 재인라인·해시·snapshot·.skill 동기화 **✅ 2026-06-21 (core hash a64604d0, .skill 258파일, staleness 0; 잔여 red=README count만→Phase 5)**
- [x] Phase 4 — 경량 W3 마감 (W3a render-audit 배선 문서화 · W3b repo hygiene · W3c manifest updated) **✅ 2026-06-21**
- [x] Phase 5 — 작성 프로토콜 + **5.10.6 버전·governance 정합 승격** (U0 흡수 + `release-approval-v5.10.6.md`) **✅ 2026-06-21 (validate --skill-dir 0 issue · governance 176/176 · completion 4/4)**

---

## Phase 0. G1~G8 재현 fixture

### 목표
- 여덟 결함이 **현행 검증을 통과**(=결함이 안 잡힘)함을 fixture로 입증해 회귀 기준선을 만든다.

### 구현 태스크
- [x] G1: 좁은 표 열에 `<td><code>100</code>`를 둔 HTML이 현행 `validate/quality`를 통과함을 보인다. → [g1_table_code_nowrap.html](fixtures_g1_g8/g1_table_code_nowrap.html); `unprotected_long_token_gate`+`table_mobile_wrapper_gate` 0건.
- [x] G3: `.try > .summary-card > a`(흰 카드 안 링크)가 현행 게이트를 통과(저대비 미검출)함을 보인다. → [g3_try_inner_card_link.html](fixtures_g1_g8/g3_try_inner_card_link.html); 실측 대비 **1.65:1**(문서 기재치 일치)인데 `try_card_contrast_guard` 비대상(summary-card·링크).
- [x] G2: wg-04 노드 2개가 좌표상 겹치는 SVG가 현행 정적 게이트를 통과함을 보인다(정적으로는 불가 확인). → [g2_wg04_node_overlap.html](fixtures_g1_g8/g2_wg04_node_overlap.html); `widget_static_gate` 0건(좌표 비대상).
- [x] G4: `.source-preserve`가 좌측 레일과 본문 텍스트 사이 gutter 없이 붙어 보여도 현행 게이트를 통과함을 보인다. → [g4_source_preserve_gutter.html](fixtures_g1_g8/g4_source_preserve_gutter.html); 해당 게이트 부재.
- [x] G5: `.mini-card > .tag:first-child + h3`가 세로 간격 없이 붙어 보여도 현행 게이트를 통과함을 보인다. → [g5_mini_card_tag_rhythm.html](fixtures_g1_g8/g5_mini_card_tag_rhythm.html); 해당 게이트 부재.
- [x] G6: TOC가 첫 콘텐츠 섹션 내부에 들어간 HTML이 현행 게이트를 통과함을 보인다. → [g6_toc_nested_in_section.html](fixtures_g1_g8/g6_toc_nested_in_section.html); `toc_map_contract_gate`는 구조만 검사(배치 비대상) 0건.
- [x] G7: `<h2>` 앞 빈 anchor/div 때문에 `section > h2:first-child`가 깨진 HTML이 현행 게이트를 통과함을 보인다. → [g7_h2_leading_anchor.html](fixtures_g1_g8/g7_h2_leading_anchor.html); 해당 게이트 부재.
- [x] G8: `.core-insight` 내부 `h3` 상단 여백이 과하거나 red gradient가 `core-insight--neutral`로 사라져도 현행 게이트를 통과함을 보인다. → [g8_core_insight_neutral.html](fixtures_g1_g8/g8_core_insight_neutral.html); 해당 게이트 부재.
- [x] 재현 fixture를 `tests/` 보조 자산 또는 dev-plan 첨부로 보관(회귀 비교용). → **dev-plan 첨부**: [fixtures_g1_g8/](fixtures_g1_g8/) (스킬 디렉터리 밖 → `.skill` byte-match 게이트 무영향) + 러너 [baseline_check.py](fixtures_g1_g8/baseline_check.py).

### 자체 테스트
- [x] 여덟 fixture 모두 "현행은 통과(=결함)"가 재현된다. → `python3 dev-plan/fixtures_g1_g8/baseline_check.py` = **8/8 INVISIBLE, exit 0**.
- [x] 기존 examples는 그대로 통과(무회귀 기준선 기록, 현 코어 해시 `a73eb204` 기준). → `skill_asset_lint` 클린 + `test_governance_gates.py` **162/162**; `git status`상 `skills/` 무변경.

### 이슈 및 수정
- [x] 발견 이슈 없음 — 여덟 fixture 모두 의도대로 현행 미검출. G3 실측 대비가 문서 기재 1.65:1과 정확히 일치.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능 — Phase 1에서 D1=코어 직접 반영으로 동일 fixture를 '검출/정상화'로 뒤집는다.

---

## Phase 1. G1·G3~G8 CSS/패턴/생성계약 하드닝 + A5 (D1=코어 직접)

### 목표
- 좁은 표 셀 코드 줄바꿈(G1), `.try` 내부 흰 카드 링크 저대비(G3), `source-preserve` 좌측 gutter 부족(G4), `mini-card` 첫 태그 vertical rhythm 부족(G5), TOC 중첩(G6), 섹션 첫 제목 상단 여백(G7), `core-insight` 내부 제목 간격·red gradient 회귀(G8), `prefers-contrast` 단서 부재(A5)를 정본 CSS/마크업/생성계약으로 닫는다.

### 구현 태스크
- [x] G3: `.try :is(.box,.summary-card,.cta-box,.card-block,.mini-card) a`에 `color:var(--accent-2)`+밑줄 reset 추가. → [components.css](../skills/adaptive-html-final/assets/components.css) (`.try a:hover` 직후). `--accent-2` 채택 근거: inner-card는 `--card` 배경(L114-122)이라 8테마 토큰 자동 적응, min 대비 **6.09:1**(아래 자체 테스트).
- [x] G1: 표 셀 짧은 코드 줄바꿈 방지. → **CSS 변경 불필요**: `.status-pill`이 이미 존재하고 `.tbl .status-pill,table .status-pill{white-space:nowrap}`(components.css:101)로 정본화돼 있음. 따라서 G1은 **작성 계약**(상태코드는 bare `<code>` 대신 `.status-pill`) — Phase 2 게이트 + Phase 5 문서로 닫는다. (broad `td code` nowrap은 예제 09 regex 다토큰 코드를 깨므로 회피.)
- [x] G4: `editorial-patterns.css`에 `.source-body-inner{border-left:1px solid var(--line);padding-left:24px;margin-left:2px}` 정본 추가. → [editorial-patterns.css](../skills/adaptive-html-final/assets/editorial-patterns.css). 생성 마크업은 inline style 대신 `.source-body-inner` 사용(작성 규칙).
- [x] G5: `.mini-card>.tag:first-child{display:inline-flex;margin-bottom:14px}` + `+h3{margin-top:0;margin-bottom:12px}` + `h3+p{margin-top:0}` 추가. → [components.css](../skills/adaptive-html-final/assets/components.css) (`.mini-card h3` 직후).
- [~] G6: 생성기는 TOC를 **독립 `<section class="document-toc-section">`** 에 삽입. → **Phase 1 CSS 없음**(순수 DOM/생성 규칙). layout은 `{{EXECUTIVE_SUMMARY}}` placeholder에 TOC가 들어가는 구조 → **Phase 2 DOM 정적 가드 + Phase 5 작성 프로토콜**로 닫는다(예제 영향은 Phase 2에서 산정).
- [~] G7: 섹션 anchor는 빈 `<div id>`가 아니라 `<h2 id>`에 직접 부여. → **Phase 1 CSS 없음**(순수 마크업 규칙). **Phase 2 DOM 정적 가드 + Phase 5 문서**.
- [x] G8: `.core-insight>:first-child{margin-top:0}` 내부 제목 margin 누수 reset 추가. → [editorial-patterns.css](../skills/adaptive-html-final/assets/editorial-patterns.css). red gradient 카드에 `core-insight--neutral` 금지는 작성 규칙 + Phase 2 게이트.
- [x] **A5**: `@media (prefers-contrast: more){:root{--ink-soft:#333;--ink-mute:#454545;--line:#b5b5ac}}` 추가. → **코어 `theme.css`** 말미(forced-colors와 동형, 대비 상향만 → 일반 렌더 영향 0). 다크/블루는 `:root:has()` 토큰이 더 구체적이라 유지.
- [x] 코어(`components.css`) 회귀 확인: `code{}` 무변경, `.try a` 직속 링크 무변경(inner-card 한정 override만 추가). `!important` 0건.

### 자체 테스트
- [~] Phase 0의 G1·G3 fixture가 이제 **실패(=검출/정상화)** 한다. → **Phase 2로 이월**: 검출 게이트가 Phase 2 산출물이고, fixture는 inline CSS로 결함을 고정하므로 자동 flip 안 됨. Phase 1은 **자산 정합**(아래)으로 검증, '검출 flip'은 Phase 2 게이트에서.
- [x] G3 fix 정합: `.try :is(...) a` specificity (0,2,1) > `.try a` (0,1,1) → inner-card 링크가 `--accent-2`로 이김(asset 규칙 존재 확인). G4/G5/G8/A5 정본 규칙 자산 내 존재 확인(`phase1_verify` PASS).
- [x] 기존 examples 콘텐츠 회귀 0 — `quality_contract_check.py examples` **OK (17 file)**. (inline-hash/`--skill-dir`는 Phase 3 재인라인 후 검증.)
- [x] `.try` 직속 링크(다크 배경)는 여전히 `--link-on-dark`(override가 inner 카드 selector에 한정됨 확인).
- [x] 8테마 전부에서 inner 카드 링크(`--accent-2` on `--card`) 대비 ≥ 4.5:1 — 실측 **min 6.09**(light/white/light2/skyblue/sepia/dark/dark2/blue). `--accent`(4.17)·`--link-on-dark`(1.53) 대비 우월.
- [~] `source-preserve` 390px 좌측 gutter/overflow, `mini-card` tag rhythm 390/1280px, `core-insight` gradient/heading **픽셀 렌더** 확인 → **Phase 2 render-audit로 이월**(전용 렌더 차수). CSS상 고정폭 없음·padding만 추가라 overflow 무유발로 정적 판단.
- [x] TOC 독립 섹션(G6)·`<h2 id>` anchor(G7) — Phase 2 DOM 게이트 + Phase 5 문서 대상(Phase 1 비CSS).
- [x] `prefers-contrast: more`는 약 토큰 대비를 올리기만 함(일반 렌더 영향 0) — 자산 규칙 확인.

### 이슈 및 수정
- [x] 발견 이슈 없음(실제 회귀). **예상된 결합 1건**: 코어 CSS 3종 변경으로 `.skill` 패키지가 워킹트리와 byte 불일치 → `skill_doc_consistency_gate`의 `skill_package_content_stale` 3건으로 거버넌스 **162→161**. 이는 **Phase 3 재인라인·재패키징**에서 해소되는 계획된 중간 상태(내용/구조/대비 회귀 아님). 그 외 `skill_asset_lint`·`theme_contrast_failures`·gate-scoped `on_accent_pairing` 전부 클린.

### 완료 조건
- [x] 구현 완료 (G3·G4·G5·G8·A5 코어 CSS; G1·G6·G7 계약/게이트 위임)
- [x] 자체 테스트 완료 (정적·대비·무회귀; 렌더·검출 flip은 Phase 2로 명시 이월)
- [x] 다음 Phase 진행 가능 → Phase 2: 정적 가드 + render-audit 신설(악성 fixture 먼저), Phase 0 fixture를 '검출'로 flip.

---

## Phase 2. G2 Playwright 마이크로 레이아웃 감사 + G4~G8 정적 가드

### 목표
- 정적으로 못 잡는 **diagram 노드 박스 overlap**(G2)과 **중첩 링크/텍스트 저대비**(G3 보강)를 렌더 감사로 검출한다. G4~G8은 CSS/마크업/DOM 정적 가드로 재발을 막는다. `render-audit.json` 계약 + `completion_check.py`에 흡수(Playwright 직접 구동은 별도 캡쳐 차수, 검증기는 아티팩트만 검사).

### 구현 태스크
- [x] 노드 overlap 측정(>4px×4px) → [scripts/micro_layout_audit.mjs](../scripts/micro_layout_audit.mjs) (Playwright). containment(포함)은 제외. `node_overlap_ok` 산출.
- [x] 저대비 측정(<4.5:1, 대형 3:1) → 같은 .mjs가 `.try` inner-card 링크 렌더색↔유효배경 대비 측정 → `inner_card_link_contrast_ok`. 기존 다크 대비 감사와 중복 회피(inner-card 컨텍스트만).
- [x] `completion_check.py`가 benchmark `micro_layout.checks`에 `node_overlap_ok`·`inner_card_link_contrast_ok` 필수화(부재/false면 실패). → [completion_check.py](../skills/adaptive-html-final/scripts/completion_check.py) `required_micro` 확장.
- [x] 외부 캡쳐용 audit 스크립트 분리 — 검증기는 결과 JSON만 읽음(무 JS 불변 유지). **G2/G3 fixture에서 실측 검출 확인**: g2 → `node_overlap_ok:false`(50×40px), g3 → `inner_card_link_contrast_ok:false`(1.65:1).
- [x] 악성 fixture로 게이트 실패 확인 — governance 자체 테스트에 catch/pass 14건 등록(아래).
- [x] G4 정적 가드: `source_preserve_gutter_gate` — editorial-patterns.css에 `.source-body-inner{padding-left}` 부재 시 실패(**asset-level**, skill_asset_lint 편입).
- [x] G5 정적 가드: `mini_card_tag_rhythm_gate` — components.css에 `.mini-card>.tag:first-child{margin-bottom}` 부재 시 실패(asset-level).
- [x] G6 정적 가드: `toc_in_executive_summary_gate` — toc-map이 `executive-summary` 섹션 자손이면 실패(**per-page DOM**). 전용 TOC 섹션/top-level 허용 → 18 예제 전부 통과 확인.
- [x] G7 정적 가드: `section_leading_empty_anchor_gate` — 섹션 첫 `<h2>` 앞 빈 anchor/div면 실패(per-page DOM). 18 예제 전부 통과 확인.
- [x] G8 정적 가드: `core_insight_heading_reset_gate` — editorial-patterns.css에 `.core-insight>:first-child{margin-top:0}` 부재 시 실패(asset-level). **G3 보강**: `try_inner_card_link_contrast_gate`(per-page) — inner-card 링크 reset 부재 시 실패(기존 `.try` p/tag/dark-link 가드 family의 link 보강).
- [x] **결정**: G8 `--neutral` 출력 검출은 의도 의존(정당한 opt-in과 구분 불가)이라 **출력 게이트 미신설** — asset reset 게이트 + Phase 5 작성 규칙으로 닫음. G1은 `.status-pill` 정본 이미 존재 → 작성 계약/Phase 5.

### 자체 테스트
- [x] 겹침·저대비 fixture가 render-audit(.mjs)에서 검출됨(위 실측).
- [x] G3~G8 정적 가드가 fixture를 검출 + 정본 자산은 통과 — `python3 dev-plan/fixtures_g1_g8/baseline_check.py` **PASS(전이 확인)**.
- [x] examples는 G6/G7 DOM 가드에 **18/18 통과**(회귀 0), `quality_contract_check examples` OK(17 file). (`node_overlap_ok`/`contrast_ok`=true는 실제 benchmark 캡쳐차수에서 .mjs가 채움 — Phase 4 W3a 배선.)
- [x] 기존 render-audit(overflow·screenshot) 계약 회귀 0(`check_render_audit` 기존 경로 무변경, micro 키만 추가).
- [x] governance 자체 테스트에 신규 게이트 catch/pass **14건** 추가(G4·G5·G8 각 2 + G3 3 + G6·G7 각 2 + G2 render 1), `manifest.quality.governance_count` **162→176** 일치. → 거버넌스 **175/176**(유일 실패 = 아래 예상 staleness).

### 이슈 및 수정
- [x] 발견 이슈 없음(실제 회귀). **예상된 중간 상태 2건**(둘 다 후속 Phase에서 해소):
  - `skill_doc_consistency_gate` red — (a) `.skill` 패키지 staleness: Phase 1·2에서 변경된 7개 스킬 파일(assets 3 + manifest + scripts 2 + tests 1) byte 불일치 → **Phase 3 재패키징**에서 해소. (b) README governance 카운트 표면(162) ↔ manifest(176) 불일치 → **Phase 5 버전·표면 동기화**에서 해소. 두 항목이 한 check()에 묶여 거버넌스 1건 실패로 표시됨.
- [x] **render_audit_fulltest.mjs 후속**: 메인 benchmark 캡쳐 생산자가 신규 2키를 emit하도록 micro_layout_audit.mjs와 배선 → **Phase 4 W3a**로 이월(현재는 micro_layout_audit.mjs --merge로 보강).

### 완료 조건
- [x] 구현 완료 (G3~G8 게이트 + G2 render-audit + .mjs 생산자 + governance 등록 + count 176)
- [x] 자체 테스트 완료 (전이 검출 PASS, examples 무회귀, 거버넌스 175/176 = 예상 staleness만)
- [x] 다음 Phase 진행 가능 → Phase 3: core-css-sha256 재리베이스 + examples 18종 재인라인 + css-integrity + .skill 재패키징(이때 staleness 7건 해소).

---

## Phase 3. examples 재인라인·해시·snapshot·.skill 동기화

### 목표
- D1=코어 직접이므로 코어 해시가 변동한다. 해시/스냅샷/패키지를 한 세트로 정합한다.

### 구현 태스크
- [x] core-css-sha256 재계산 `a73eb204` → **`a64604d0…e6c2e02`** + examples 18종 인라인 코어 CSS·해시 마커 재주입(surgical: OLD asset bytes→NEW + hash 마커, 본문 무변경, 각 +1693B).
- [x] `examples/sources/css-integrity.json` 재생성 — `core_css_sha256` + 변경 자산 `asset_sha256`(theme/components/editorial-patterns) 갱신.
- [x] `examples/sources/assets/*.css` 변경 자산 동기화(theme·components·editorial-patterns 3종).
- [x] `examples/sources/adaptive-html-final-manifest.json` = 현행 manifest와 byte-equal(governance_count 176 반영본 복사).
- [x] `skills/adaptive-html-final.skill` 재패키징(캐시류 제외, 워킹트리 byte-match) — 258 파일 / 2,861,610 B.

### 자체 테스트
- [x] `validate_output.py examples --skill-dir` — **inline hash·verbatim·snapshot·.skill byte-match 전부 통과**. 잔여 issue 3건은 README count 표면(162)뿐 = Phase 5. (해시/verbatim/패키지 issue 0)
- [x] 신규 코어 해시가 examples 18종·css-integrity·`.skill`에 일관 반영 — `skill_package_content_stale` **0**(staleness 7건 해소), `a73eb204` 본문 외 잔여 0.
- [x] `quality_contract_check examples` OK(17 file) — 재인라인이 CSS만 교체, 본문 무변경 확인.

### 이슈 및 수정
- [x] 발견 이슈 없음. (재인라인 스크립트 안전장치: 자산별 OLD bytes count==1 검증 후 치환, write 전 전수 assert. NEW theme.css가 OLD를 prefix로 포함하는 케이스만 assert 보정.)

### 완료 조건
- [x] 구현 완료 (재인라인·해시·sources·.skill 정합)
- [x] 자체 테스트 완료 (`--skill-dir` 패키지/해시/verbatim 전부 OK, staleness 0)
- [x] 다음 Phase 진행 가능 → Phase 4(경량 W3) → Phase 5(README count 176 + 5.10.6 버전 표면 일괄 = 마지막 red 해소).

---

## Phase 4. 경량 W3 마감 (render-audit 배선 · repo hygiene · manifest updated)

### 목표
- 5.10.6에 포함하기로 한 경량 W3 항목을 닫는다(스킬 본체 기능 외 정합/문서/위생).

### 구현 태스크
- [x] **W3a**: SKILL.md L435에 캡처 명령 명시 — `node scripts/render_audit_fulltest.mjs <dir>` 후 `node scripts/micro_layout_audit.mjs <dir> --merge`(G2 node_overlap·G3 inner-card 대비를 micro_layout.checks에 병합). vendoring 불필요(repo scripts 유지, 검증기는 JSON만 읽음).
- [x] **W3b**: untracked `build_*.py` 11개 → **본 릴리스 비대상으로 결정**. orginal_skill 출력 생성용이며 스킬 본체/버전과 독립 → orginal_skill 통합 워크스트림([implement_20260621_130535.md](implement_20260621_130535.md))에서 처리. gitignore/커밋 강제 안 함(사용자 커밋 권한).
- [x] **W3c**: `manifest.json` `updated` `2026-06-15` → `2026-06-21`.

### 자체 테스트
- [x] SKILL.md만 보고 render-audit 캡처 재현 가능(L435 명령·산출 경로 명확).
- [x] `git status` 스킬 본체 의도 변경만(W3b build_*.py는 의도적 비대상으로 분리).
- [x] `manifest.json` `updated` = 2026-06-21.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

---

## Phase 5. 작성 프로토콜 + 5.10.6 버전·governance 정합 승격

### 목표
- G1~G8 재발 방지 규칙을 문서화하고, U0를 흡수해 `5.10.6` 버전/거버넌스 표면을 단일 출처로 일괄 동기화한다.

### 구현 태스크
- [x] `docs/adaptive-html-final-template-authoring-protocol.md` §3.6에 G1~G8 "실패→정답" 추가(.status-pill, wg-04 노드 간격, inner-card 링크 `--accent-2`, source-preserve `.source-body-inner`, mini-card tag 리듬, TOC 독립 섹션, `<h2 id>` anchor, core-insight gradient 보존 + reset).
- [x] **U0 흡수**: `CHANGELOG.md` `## Unreleased` → **`## v5.10.6` 정식 엔트리**(배지 padding + G1~G8 + A5 + 신규 게이트, 코어 해시 `7e151665`→`a64604d0`). v5.10.5 역사 노트 보존.
- [x] `dev-plan/release-approval-v5.10.6.md` 작성.
- [x] manifest/SKILL/README(repo+skill)/AGENTS/Guide/visual-html-system/examples footer/source snapshot 버전 표면 일괄 `5.10.6`(현행 surface만, 역사 항목 보존).
- [x] `manifest.quality.governance_count` 162→**176** + README 게이트 현황/표/트리/배지 수치 동기화.
- [x] 버전 표면 스캔: 현행 5.10.5 잔여 0(역사 changelog row·demo17 캡처 섹션·manifest 역사 entry만 의도적 보존).

### 자체 테스트
- [x] `test_governance_gates.py` = **176/176**(신규 count = manifest).
- [x] `validate_output.py examples --skill-dir` **0 issue**(버전·README·hash·verbatim·snapshot·.skill byte-match 전부).
- [x] `quality_contract_check.py` OK(17 file) · `completion_check.py` **4/4 OK**.
- [x] README badge/NOTE/표/트리 governance 수치 = manifest count(176).
- [x] `CHANGELOG.md` `## Unreleased` 잔여 0(v5.10.6 승격).
- [x] 버전 표면 게이트(`output_visible_version_stale`·`readme_*`·`skill_md_version_mismatch`·`manifest_version_*`·`skill_package_*`·`version_release_approval`) 전부 통과.

### 이슈 및 수정
- [x] **외부 편집기/린터 revert 이슈**: 작업 중 IDE(또는 hook)가 일부 스킬 파일(SKILL.md·CHANGELOG·examples·sources·README·visual-html-system)을 git HEAD(a73eb204/5.10.5)로 간헐 revert. 전부 재적용해 최종 on-disk 상태를 5.10.6/176/a64604d0로 정합. **커밋 전 IDE에서 해당 파일을 닫거나 revert 동작을 끄고 재확인 권장**(미커밋 상태라 재-revert 위험).

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료 (V/Q/C/governance 전부 green)
- [x] 전체 워크스트림 완료 (사용자 커밋 요청 대기)

### 전문가 리뷰 (적대적, 4-리뷰어 + 종합) — 판정 **SHIP-with-notes**
- HIGH 0 · BLOCK 0. CSS-fix=PASS(8테마 G3 min 6.09:1·specificity·A5 안전·!important 0 독립 재계산), 게이트/버전표면/무회귀=PASS-with-notes. validate --skill-dir 0 issue·governance 176/176·완료 4/4·.skill byte-match·revert 0 모두 리뷰어 독립 재현.
- **M1 (수정 완료)**: 이전 코어 해시 prose `7e151665`→실제 직전 baseline `a73eb204`. CHANGELOG L15·README 567/594·release-approval L12를 `a73eb204→a64604d0`로 정정(manifest와 일치). `7e151665`은 'v5.10.5 태그' 역사 맥락으로만 잔존.
- **M2 (커밋 시 처리)**: `scripts/micro_layout_audit.mjs`(G2/G3 render-audit 생산자)는 repo-side 캡처 도구(`render_audit_fulltest.mjs`와 동일 관례, repo `scripts/`)이며 현재 **git untracked** → 릴리스 커밋 시 함께 추가.
- LOW(비차단, 정적층 한정·render-audit가 최종 방어): G3 게이트 `text-decoration-color` 앵커 우회, G6/G7 off-contract(div-scoped exec-summary·span/a-name anchor) false-negative, G4/G5/G8 exact-form 정규식. 현 정본 자산/authoring 경로상 도달 불가 → 후속 하드닝 선택.

### 추가 선검토 — 2026-06-21 품질 회귀 가능성

5.10.6 본체 examples 게이트는 green이지만, 새 render-audit 생산자 자체의 수집 범위에서 false green 가능성이 확인되었다. 이 항목은 기존 examples 내용 자체의 실패는 아니지만, **micro-layout gate를 hard gate로 신뢰하려면 패치 전 릴리스/승격 판단에 쓰면 안 된다.** 따라서 이후 `pretest/custom HTML 우회 차단` 워크스트림의 **Phase -1 최우선 패치**로 승격한다.

| ID | 선검토 결과 | 회귀 가능성 | 조치 |
|---|---|---|---|
| R0 | `node scripts/micro_layout_audit.mjs skills/adaptive-html-final/examples`가 `index.html`만 검사할 수 있음 | 17개 content HTML의 G2/G3 render-only 결함이 directory run에서 누락될 수 있음 | `micro_layout_audit.mjs` 디렉터리 입력 시 `index.html + sibling *.html + pages/*.html` 전부 수집하도록 패치. 단일-page output은 계속 허용 |
| R1 | examples 18개를 파일별로 반복 실행하면 모두 micro check 통과 | 현재 공식 examples에는 즉시 G2/G3 회귀 없음 | 패치 후 일괄 directory run에서도 18개 전체 통과를 증명 |
| R2 | `output/2026-06-14/codex-5.10.4-independent-17-benchmark`는 새 micro key 부재로 completion fail | 과거 benchmark를 새 기준으로 소급 검증하면 stale evidence fail 증가 | 과거 output은 historical로 유지하고, official 승격 대상에만 새 key 요구 |

추가로, `scripts/micro_layout_audit.mjs`가 untracked 상태로 남으면 `completion_check.py`는 새 key를 요구하지만 생산자 스크립트가 커밋에서 빠질 수 있다. 따라서 릴리스/후속 패치 커밋 전 **producer 포함 여부와 directory glob 패치 적용 여부**를 `git status --short`와 staging 목록에서 재확인한다.

> 2026-06-21 추가 상태(역사): mode18 partial skeleton이 스킬 트리에 처음 들어온 빌드 중간 시점에는 dirty worktree의 registry/validate/governance/completion이 green이 아니었다. 이는 5.10.6 G1~G8 본체 패치 자체의 실패가 아니라 신규 mode18 부분 구현의 정합 blocker였다.
> **✅ 2026-06-21 마감(현행): mode18 정합 마감 완료 → green 복귀.** governance 185/185·`validate --skill-dir` 0·completion 4/4·registry sync OK·코어 해시 `a64604d0` 불변. `scripts/micro_layout_audit.mjs` directory glob 패치(index만 검사 → index∪pages∪형제 *.html 합집합) 적용·19개 예제 실측 결함 0·staging 포함 확인 완료(L325 점검 항목 해소).

---

## 다음 할 일 (5.10.6 배치 트래커)

batch 트리거 발동(2026-06-21). 아래는 5.10.6 차수 항목과 status. (⏳대기 / ▶진행 / ✅완료)

| ID | 항목 | 반영 위치(예상) | 코어 변경? | Phase | status |
|---|---|---|---|---|---|
| G1 | 좁은 표 열 짧은 코드 nowrap | `components.css` / 작성 규칙 | 예(D1) | 1 | ▶ |
| G2 | wg-04 노드 overlap Playwright 감사 게이트 | `completion_check.py` + `render-audit.json` + audit 스크립트 | ✕(게이트, governance++) | 2 | ▶ |
| G3 | `.try` 중첩 흰 카드 링크색 reset | `components.css` | 예(D1) | 1 | ▶ |
| G4 | `source-preserve` 좌측 gutter 정본화 | `editorial-patterns.css` + 정적 가드 | 예(조건부 자산) | 1·2 | ▶ |
| G5 | `mini-card` 첫 tag와 h3/p vertical rhythm | `components.css` + 정적 가드 | 예(D1) | 1·2 | ▶ |
| G6 | TOC 독립 섹션 생성 | 생성 절차 / layout placeholder / DOM 가드 | ✕(절차+게이트) | 1·2 | ▶ |
| G7 | 섹션 anchor `<h2 id>` 직접 부여 | 생성 마크업 / DOM 가드 | ✕(절차+게이트) | 1·2 | ▶ |
| G8 | `core-insight` 제목 margin reset + red gradient 보존 | `editorial-patterns.css` + 정적 가드 | 예(조건부 자산) | 1·2 | ▶ |
| **U0** | **Unreleased 코어 변경 5.10.6 버전 흡수** | `CHANGELOG.md` + `manifest.json` + release-approval | 예(이미 착지) | 5 | ▶ |
| **A5** | **`prefers-contrast` 단서 추가** | `theme.css`(코어) 또는 조건부 자산 | 구현 시 결정 | 1 | ▶ |
| **W3a** | **render-audit 캡처 배선 문서화** | `SKILL.md` / docs (+선택 vendoring) | ✕(문서) | 4 | ▶ |
| **W3b** | **untracked `build_*.py` 처리 방침** | `.gitignore` / `scripts/` | ✕(repo 위생) | 4 | ▶ |
| **W3c** | **`manifest.json` `updated` 동기화** | `manifest.json` | ✕(비게이트 필드) | 4 | ▶ |
| B1 | 강화 micro-layout audit(단색 rail unique-count 등) | audit 스크립트 + 게이트 | ✕(게이트) | 2(선택) | ⏳ |
| B2 | 모드별 Mode Build Sheet / build-evidence.json 분리 | 생성 절차 문서 | ✕(절차) | 5(선택) | ⏳ |
| B3 | 17모드 순차 runbook 문서 | `docs/adaptive-html-final-17-mode-sequential-runbook.md` | ✕(문서) | — | ✅ **이미 존재(2026-06-17)** — 정합 확인만 |

### 5.10.6 제외 (별도 차수/로드맵)

| ID | 항목 | 행선지 |
|---|---|---|
| W2 | strategy-os 모드 18 (wg-21~28 등) | **`5.11.0` minor** — [implement_20260620_201946.md](./implement_20260620_201946.md) |
| R1 | theme-token 마이그레이션(테마 6종·theme.css 분할) | 별도 로드맵 — [docs/theme-token-strategy.md](../docs/theme-token-strategy.md) |
| R2 | AGENTS §2.2 HTML 생성비용 최적화 | 별도 대형 워크스트림(deferred) |
| R3 | export_output.mjs 정식 재검증 + PNG/WebP·로컬 폰트 후속 | [implement_20260606_072804.md](./implement_20260606_072804.md) |
| R4 | output 공개/보존 정책 문서화 | [implement_20260612_221316.md](./implement_20260612_221316.md) |
| R5 | visual-html 잔여 토큰화(wg-09 scroll-snap/wg-13/wg-20) | [implement_20260612_221316.md](./implement_20260612_221316.md) |

### 즉시 다음 할 일

1. **트리거 발동 완료** — 본 문서가 5.10.6 마스터 플랜이다. 사용자 코드 착수 지시 시 Phase 0부터 진행.
2. **착수 순서**: Phase 0(재현 fixture) → Phase 1(CSS 하드닝+A5) → Phase 2(감사 게이트+정적 가드) → Phase 3(재인라인/해시/.skill) → Phase 4(경량 W3) → Phase 5(프로토콜+5.10.6 승격+release-approval).
3. **mode 18은 5.10.6 릴리스 후 5.11.0 차수**로 별도 착수.

## 문서 기반 제약 사항

- 무 JS 불변(JSON-LD만). 감사 스크립트는 외부 캡쳐 차수에서 구동하고 출력 HTML에 JS를 넣지 않는다.
- §0.6 결정표·`MODE_TEMPLATE_CONTRACTS`·`widget-system.md` 3자 정합을 깨지 않는다.
- 코어 CSS 변경 시 버전 표면 게이트(`output_visible_version_stale`·`readme_*`·`skill_package_*`)를 한 세트로 갱신.
- U0: manifest의 v5.10.5 릴리스 노트 내 `7e151665` 문자열은 **역사 기록**이므로 편집하지 않는다. 5.10.6은 신규 엔트리로 추가한다.

## 잔여 리스크 / 후속 과제

- G1 일반화의 오탐: 표 셀 긴 인라인 코드까지 nowrap되면 가로 overflow 유발 → 범위를 짧은 코드/첫 열/`.status-pill`로 좁히는 것이 관건.
- G2 감사 비용: Playwright 캡쳐 차수 의존 → 신규 산출물·examples에만 요구, 과거 output에 소급하지 않는다.
- G2/G3 audit 생산자 수집 범위: 디렉터리 입력에서 `index.html`만 검사하면 false green 가능 → `micro_layout_audit.mjs` directory glob 패치를 후속 Phase -1에서 최우선 처리.
- G6~G8은 개별 산출물 inline 보정으로 해결되기 쉬워 본체 회귀로 남을 위험 → 생성 프로토콜 + DOM 정적 가드 병행.
- A5 위치(코어 theme.css vs 조건부): 코어면 해시 영향, 조건부면 불변 — Phase 1에서 측정·결정·기록.
- 코어 해시는 Phase 3에서 최종 1회 재리베이스(`a73eb204` → 5.10.6 최종 해시). 중간 해시가 잔류하지 않게 examples/css-integrity/.skill 일괄 점검.
