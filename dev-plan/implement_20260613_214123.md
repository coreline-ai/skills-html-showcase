# implement_20260613_214123.md

작성 일시: `2026-06-13 21:41:23 KST`
최종 수정: `2026-06-13 21:55 KST` (리뷰 반영 — P1 toc 동적 모델·추출 정본 고정, P2 임계 역산·의미 비교·recipe 매핑·bridge deep-equal, P3 B-lite/B-full 분할)

이 문서는 `adaptive-html-final` 17개 모드를 **B안: Registry 기반 모듈화 구조**로 개편하기 위한 개발 범위를 고정하고, 신규 모드 추가가 문서·검증기·예제 정합성 회귀로 이어지지 않도록 하기 위한 작업 문서다.

## 개발 목적

현재 `adaptive-html-final`은 layout·recipe·example 자산은 17개로 분리되어 있지만, 모드 정의 자체는 `AGENTS.md`, `SKILL.md`, `manifest.json`, `references/*`, `validate_output.py`, `test_governance_gates.py`, README 계열 문서에 중복 분산되어 있다.

이번 개발의 목적은 모드 정의를 단일 Registry로 모듈화하여 다음 상태를 만든다.

- 신규 모드 추가 시 수정 표면을 `mode registry + layout + recipe + 선택 reference/example` 중심으로 축소한다.
- `SKILL.md`·`AGENTS.md`·`manifest.json`·validator·examples index가 서로 다른 모드 정보를 갖는 회귀를 차단한다.
- 현재 17개 모드의 생성 계약, vt/wg 결정표, layout mapping, 품질 게이트는 유지한다.
- 승인 없는 버전 상승 없이 `adaptive-html-final v5.10.3` 기준으로 정합한다.

## 개발 범위

- `skills/adaptive-html-final/modes/NN-<mode>.json` 신설. 단일 `mode-registry.json` 방식은 사용하지 않는다.
- 17개 기존 모드 정의를 Registry로 추출.
- Registry schema 정의 및 검증기 추가.
- `manifest.json.modes`와 Registry 동기화 검증.
- `validate_output.py`의 mode→layout/vt/wg 계약 일부를 Registry 기반으로 전환.
- `AGENTS.md`·`SKILL.md` 결정표와 Registry의 불일치를 잡는 sync gate 추가.
- `examples/index.html` 또는 예제 목록이 Registry와 맞는지 검증.
- governance 테스트에 Registry 정합 게이트 추가.
- 신규 모드 추가 runbook 문서화.

## 제외 범위

- 18번째 실제 신규 모드 추가.
- 기존 17개 examples 본문 대규모 재작성.
- 디자인 토큰, CSS 시각 스타일, 8테마 구조 변경.
- `manifest.version` 변경 또는 `5.10.4` 승격.
- `.skill` 패키지 재생성은 실제 소스 변경 후 최종 단계에서만 수행한다. 문서 계획 작성만으로는 재패키징하지 않는다.
- 과거 `output/` 전체를 스킬 정본으로 승격하지 않는다. `output/`은 테스트/공개 산출물이며 Registry 입력이 아니다.

## 참조 문서

- [AGENTS.md](../AGENTS.md)
- [skills/adaptive-html-final/SKILL.md](../skills/adaptive-html-final/SKILL.md)
- [skills/adaptive-html-final/manifest.json](../skills/adaptive-html-final/manifest.json)
- [skills/adaptive-html-final/references/mode-selection.md](../skills/adaptive-html-final/references/mode-selection.md)
- [skills/adaptive-html-final/references/layout-system.md](../skills/adaptive-html-final/references/layout-system.md)
- [skills/adaptive-html-final/references/visual-html-system.md](../skills/adaptive-html-final/references/visual-html-system.md)
- [skills/adaptive-html-final/references/widget-system.md](../skills/adaptive-html-final/references/widget-system.md)
- [skills/adaptive-html-final/scripts/validate_output.py](../skills/adaptive-html-final/scripts/validate_output.py)
- [skills/adaptive-html-final/tests/test_governance_gates.py](../skills/adaptive-html-final/tests/test_governance_gates.py)
- [docs/adaptive-html-final-version-release-guide.md](../docs/adaptive-html-final-version-release-guide.md)
- 이전 관련 계획: [dev-plan/implement_20260613_075749.md](./implement_20260613_075749.md)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 버전은 `manifest.json`의 현재 값 `5.10.3`을 유지한다. 승인 없는 `5.10.4` 승격 금지.
- Registry 도입 후에도 기존 17개 모드의 visible output 계약은 후퇴시키지 않는다.
- Registry는 문서 편의를 위한 보조물이 아니라 **mode 정의의 단일 출처**가 되어야 한다.
- 기존 문서의 역사적 설명은 보존하되, “현행 기준”으로 읽히는 표는 Registry와 일치해야 한다.
- 검증 OK만으로 완료하지 않는다. governance, examples validate, quality, completion, package byte-match까지 최종 확인한다.


## 진행 단계 분할 (리뷰 반영 — B-lite 먼저)

validator(2,000줄 `validate_output.py`) 전환은 이 성숙·완전 게이트화된 스킬에서 회귀 위험이 가장 크고, 그 보상("18번째 모드 추가 용이")은 **제외 범위 + 역사적으로 드문** 작업이다. 따라서 드리프트 방지 가치는 저위험 단계에서 먼저 확보하고, validator 수술은 실제 신규 모드가 필요해질 때 착수한다.

| 단계 | 포함 Phase | 성격 | 가치 / 위험 |
|---|---|---|---|
| **B-lite** | Phase 0·1·2·4 | Registry를 **검증 미러(sync gate)** 로만 도입. validator 동작은 불변 | 매 편집 드리프트 방지 대부분 확보 / **validator 회귀 위험 0** |
| **B-full** | Phase 3·5·6·7 | validator 공통 계약을 Registry 구동으로 전환 + runbook | 신규 모드 추가 비용 축소 / validator 수술 위험(통제됨) |

**기본 진행 = B-lite 완료까지.** B-full(Phase 3 이후)은 (a) 실제 18번째 모드 필요 또는 (b) 사용자 명시 지시가 있을 때만 착수한다. B-lite 동안 validator는 **읽지 않고 비교만** 한다(byte/semantic mirror).

## 고정 결정 요약

| 항목 | 고정값 | 이유 |
|---|---|---|
| Registry 저장 방식 | `skills/adaptive-html-final/modes/NN-<mode>.json` 17개 파일 | 신규 모드 diff가 작고 파일 단위 리뷰가 가능함 |
| 단일 JSON | 사용하지 않음 | 충돌·대량 diff·부분 리뷰 어려움 |
| manifest 역할 | `id + layout` 중심 최소 공개 메타 (Registry의 **파생/검증 투영**) | manifest가 계약 본문까지 떠안아 중복 정본이 되는 것을 방지 |
| 상세 계약 정본 | Registry | vt/wg/toc/layout/examples/quality/custom 적용 대상을 한곳에 고정 |
| 추출 정본 출처 | **`decision_table_consistency_gate` PASS 상태의 SKILL §0.6 ↔ `MODE_TEMPLATE_CONTRACTS` ↔ widget-system.md 트리오** | 이미 일치 강제된 트리오를 정본으로 추출해 틀린 값 고착 방지(P1-2) |
| toc 계약 모델 | **정적 불리언 아님.** `{ "rule": "always" \| "structural_h2_gte_4" }` 동적 규칙 | 현행 게이트 `is_required = is_analysis or h2_count>=4`(validate:1712)를 그대로 표현, C3 회귀 방지(P1-1) |
| quality 임계값 | **현행 게이트에서 역산한 값만** 기록 | 게이트에 없는 임계(`min_links` 등) 발명 = 행동 변경이므로 금지(P2-1) |
| 문서 동기화 | marker block **의미 비교**(표 파싱 후 셀 값) | byte 비교는 1바이트 포맷차로 전부 실패 → 취약(P2-2) |
| validator 전환 범위 | 공통 계약만 Registry화 (B-full) | custom 의미 검증 일반화로 인한 품질 회귀 방지 |
| validator 전환 전제 | registry-생성 `MODE_TEMPLATE_CONTRACTS`가 현행 dict와 **deep-equal**임을 테스트로 assert 후에만 전환 | bridge 무회귀 증명(P2-4) |
| completion 연동 | completion 단계 수 유지, governance 안에 Registry sync 포함 | 4/4 완료 흐름을 흔들지 않고 회귀 게이트만 추가 |
| 버전 | `5.10.3` 유지 | 사용자 승인 없는 `5.10.4` 승격 금지 |

## 현재 문제 요약

| 영역 | 현재 상태 | 문제 |
|---|---|---|
| 모드 목록 | `manifest.json.modes`에 `id + layout`만 존재 | vt/wg/trigger/required block 정보 부족 |
| 결정표 | `AGENTS.md`, `SKILL.md`, references에 반복 | 신규 모드 추가 시 표류 가능 |
| 검증기 | `validate_output.py`의 `MODE_TEMPLATE_CONTRACTS` 등 하드코딩 | 신규 모드 추가 때 Python 코드 직접 수정 필요 |
| 특수 계약 | YouTube/Manual/GitHub feature gate가 함수로 고정 | custom gate와 공통 gate 경계 불명확 |
| 예제 | 17개 HTML과 index 수동 동기화 | 모드 순서·파일명·visible version 표류 가능 |
| 문서 | README/Guide/CHANGELOG/AGENTS/SKILL에 현행 설명 분산 | 버전·모드 수·게이트 수 드리프트 위험 |

## 목표 아키텍처

```text
skills/adaptive-html-final/
  modes/
    01-skill-audit.json
    02-platform-blog.json
    ...
    17-checklist-playbook.json

  scripts/
    mode_registry.py
    check_mode_registry_sync.py
    sync_mode_docs.py          # 선택: 생성보다 검증 우선
    validate_output.py         # Registry 기반 계약 일부 적용
    completion_check.py

  assets/layouts/
  recipes/
  references/
  examples/
```

### Registry 고정 schema

> **고정 결정:** Registry 정본은 `skills/adaptive-html-final/modes/NN-<mode>.json` 17개 파일이다. `manifest.json`, `SKILL.md`, `AGENTS.md`, references, validator, examples는 Registry와 불일치하면 실패해야 한다. 단일 `mode-registry.json`은 채택하지 않는다.

```json
{
  "id": "landing_brief_html",
  "priority": 16,
  "label": "Landing Brief",
  "layout_class": "layout-landing",
  "layout_file": "landing-brief.html",
  "recipe": "recipes/landing.prompt.md",
  "triggers": ["랜딩", "소개 페이지", "요약 페이지"],
  "required_blocks": ["hero", "value_props", "how_it_works", "faq", "cta"],
  "primary_vt": "hero-map",
  "vt_candidates": ["hero-map", "card-grid", "feature-flag", "soft-workflow-map"],
  "vt_markers": ["vt-hero-map"],
  "wg_candidates": ["wg-02", "wg-05", "wg-08", "wg-09", "wg-16"],
  "wg_markers": ["wg-02-", "wg-05-", "wg-08-", "wg-09-", "wg-16-"],
  "toc_contract": {
    "rule": "structural_h2_gte_4",
    "forbid_plain_text_toc": true
  },
  "layout_placeholders": ["hero", "value_props", "how_it_works", "faq", "cta"],
  "examples": [
    {
      "file": "examples/12_landing_pagercalm_brief.html",
      "purpose": "reference"
    }
  ],
  "quality_contract": {
    "requires_body_icon": true,
    "requires_header_lens": true,
    "requires_render_audit": true,
    "mode_depth_min_avg": 400
  },
  "custom_contracts": []
}
```

> **스키마 주의 (리뷰 반영):**
> - `recipe`는 `<mode_id>.prompt.md`가 아니라 **실제 파일명**을 적는다(예: `skill_audit`→`recipes/audit.prompt.md`, `platform_blog`→`recipes/platform.prompt.md`). Phase 1에서 17개 매핑을 실측 확인한다(P2-3).
> - `toc_contract.rule`은 현행 게이트(`is_required = is_analysis or h2_count>=4`)를 그대로 표현한다. 분석 모드(github/github_feature/youtube/manual)=`"always"`, 그 외=`"structural_h2_gte_4"`. **정적 `requires_toc_map` 불리언·`min_links`는 쓰지 않는다**(현행 게이트에 없음, P1-1).
> - `quality_contract`에는 **현행 게이트에서 역산한 임계만** 넣는다. `mode_depth_min_avg`(현행 400)는 실재, `min_sections`/`min_pattern_kinds` 등은 대응 게이트가 있을 때만 추가한다(없으면 발명 금지, P2-1).
> - `examples.file`은 와일드카드가 아니라 **실제 파일명**으로 고정한다.

### 문서 동기화 방식 고정

문서 전체를 파싱하지 않고, Registry에서 생성되거나 검증되는 표 구간만 marker block으로 한정한다. 예:

```md
<!-- AHF:MODE_TABLE_START -->
... Registry 기반 모드 표 ...
<!-- AHF:MODE_TABLE_END -->
```

- `SKILL.md`, `AGENTS.md`, `references/mode-selection.md`, `references/visual-html-system.md`, `references/widget-system.md`의 현행 결정표는 marker block 내부만 sync 대상이다.
- 역사 설명, CHANGELOG성 문장, 배경 설명은 sync 대상에서 제외한다.
- marker block 밖의 자유 문구를 이유로 실패시키지 않는다. 단, “현행/현재” 표면이 Registry와 충돌하면 별도 textual drift gate로 실패시킨다.
- **비교 방식 = 의미 비교(P2-2).** marker block을 **표로 파싱해 셀 값(모드 id·layout·priority·primary vt·wg 후보)을 Registry와 대조**한다. byte/문자열 일치를 요구하지 않는다(열 정렬·공백·줄바꿈 등 포맷 차이로 전부 실패하는 취약성 회피). 표 렌더 포맷은 사람이 유지하고, 검증기는 값만 본다.
- **마커 삽입은 1회성 별도 태스크.** 기존 표를 `<!-- AHF:MODE_TABLE_START -->`/`<!-- AHF:MODE_TABLE_END -->`로 감싸는 작업은 Phase 4에서 문서별로 명시 수행하며, 삽입 시 표 내용은 변경하지 않는다(정합 검증과 분리).

## 예상 영향 범위

| 파일/경로 | 변경 유형 | 리스크 |
|---|---|---|
| `skills/adaptive-html-final/modes/*.json` | 신규 | 낮음 |
| `scripts/mode_registry.py` | 신규 | 낮음 |
| `scripts/check_mode_registry_sync.py` | 신규 | 중간 |
| `scripts/validate_output.py` | 수정 | 높음 |
| `tests/test_governance_gates.py` | 수정 | 중간 |
| `manifest.json` | 최소 메타 유지 + sync 검증 | 중간 |
| `SKILL.md` | marker block 표면 정리 | 중간 |
| `AGENTS.md` | marker block 표면 정리 | 중간 |
| `references/*.md` | 정본/파생 문구 정리 | 중간 |
| `examples/index.html` | 생성/검증 대상 | 낮음~중간 |
| `skills/adaptive-html-final.skill` | 최종 재패키징 | 중간 |

## Phase 상태 요약

**[B-lite] = 기본 진행 / [B-full] = 신규 모드 필요 또는 명시 지시 시 착수**

- [ ] Phase 0 완료 — 현행 중복 표면 감사 및 baseline 고정 **[B-lite]**
- [ ] Phase 1 완료 — Registry schema와 17개 모드 JSON 작성 **[B-lite]**
- [ ] Phase 2 완료 — Registry loader와 sync checker 구현 **[B-lite]**
- [ ] Phase 4 완료 — 문서/manifest/examples sync gate 추가 **[B-lite]** (Phase 3보다 먼저 수행)
- [ ] ── B-lite 게이트: 위 4개 완료 + 전체 검증 OK 시 드리프트 방지 가치 확보 ──
- [ ] Phase 3 완료 — validator의 공통 mode contract Registry화 **[B-full]**
- [ ] Phase 5 완료 — governance·completion 통합 및 패키지 정합 **[B-full]**
- [ ] Phase 6 완료 — 신규 모드 추가 runbook 검증 **[B-full]**
- [ ] Phase 7 완료 — 최종 문서·검증·리스크 정리 **[B-full]**

---

## Phase 0. 현행 중복 표면 감사 및 baseline 고정 [B-lite]

### 목표
- 현재 17모드 정의가 어디에 흩어져 있는지 파일 단위로 고정한다.
- 변경 전 검증 상태를 baseline으로 저장한다.
- **추출 정본 출처를 고정한다(P1-2).**

### 구현 태스크
- [ ] **추출 정본 고정(P1-2):** `decision_table_consistency_gate`가 PASS임을 먼저 확인하고, 그 상태의 **SKILL §0.6 ↔ `MODE_TEMPLATE_CONTRACTS`(validate:565) ↔ widget-system.md 트리오**를 Registry 추출의 단일 정본으로 선언한다. PASS가 아니면 추출 전 불일치부터 해소(이 Phase에 기록).
- [ ] 현행 toc 게이트 모델 확인: `is_required = is_analysis or h2_count>=4`(validate:1712)와 분석 allowlist(`analysis_required`)를 추출해 `toc_contract.rule` 매핑표를 만든다.
- [ ] `manifest.json`의 `version`, `modes`, `quality.governance_count` 확인.
- [ ] `AGENTS.md`, `SKILL.md`, `references/*`, `validate_output.py`, `test_governance_gates.py`의 모드명 출현 위치 목록화.
- [ ] 기존 17개 layout 파일과 recipe 파일 수를 확인한다.
- [ ] 현재 `MODE_TEMPLATE_CONTRACTS`와 `SKILL.md` 현행 결정표를 비교한다. Registry 도입 후에는 이 표를 marker block으로 전환한다.
- [ ] custom gate 대상 모드(`youtube_analysis`, `manual_analysis`, `github_feature_usage`, `github_analysis`)를 분류한다.

### 자체 테스트
- [ ] `python3 skills/adaptive-html-final/scripts/validate_output.py skills/adaptive-html-final/examples --skill-dir skills/adaptive-html-final`
- [ ] `python3 skills/adaptive-html-final/scripts/quality_contract_check.py skills/adaptive-html-final/examples`
- [ ] `python3 skills/adaptive-html-final/tests/test_governance_gates.py`
- [ ] `python3 skills/adaptive-html-final/scripts/completion_check.py skills/adaptive-html-final/examples`
- [ ] `git diff --check`

### 이슈 및 수정
- [ ] 발견 이슈 없음 또는 이 Phase에 기록 후 수정

### 완료 조건
- [ ] 변경 전 baseline이 문서에 기록됨
- [ ] 17개 모드의 중복 표면 목록이 확정됨
- [ ] 다음 Phase 진행 가능

---

## Phase 1. Registry schema와 17개 모드 JSON 작성 [B-lite]

### 목표
- 기존 17개 모드의 결정표 정보를 **Phase 0에서 고정한 정본 트리오**에서 Registry로 추출한다.
- Registry가 현행 스킬의 단일 출처 후보가 되도록 충분한 필드를 가지되, **현행 게이트에 없는 임계는 발명하지 않는다.**

### 구현 태스크
- [ ] `skills/adaptive-html-final/modes/` 디렉터리 생성.
- [ ] `01-skill-audit.json` ~ `17-checklist-playbook.json` 작성. 파일명 규칙은 `NN-<kebab-mode>.json`으로 고정한다.
- [ ] 각 JSON에 `id`, `priority`, `label`, `layout_class`, `layout_file`, `recipe`, `triggers`, `required_blocks`, `primary_vt`, `vt_candidates`, `vt_markers`, `wg_candidates`, `wg_markers`, `toc_contract`, `layout_placeholders`, `examples`, `quality_contract`, `custom_contracts` 포함.
- [ ] `layout_file`이 `assets/layouts/`에 존재하는지 확인.
- [ ] **recipe 파일명 실측 매핑(P2-3):** recipe는 `<mode_id>.prompt.md`가 아니다(`skill_audit`→`audit.prompt.md` 등). 17개 모드↔실제 `recipes/*.prompt.md` 매핑표를 만들고 각 `recipe` 필드에 실제 파일명을 적고 존재를 확인한다.
- [ ] **toc_contract 동적 매핑(P1-1):** Phase 0 매핑표대로 분석 4모드=`"always"`, 그 외=`"structural_h2_gte_4"`. 정적 `requires_toc_map`/`min_links` 금지.
- [ ] **quality_contract 임계 역산(P2-1):** 현행 게이트가 강제하는 값만 기록(`mode_depth_min_avg` 현행 400 등). 대응 게이트 없는 임계 필드는 넣지 않는다.
- [ ] `primary_vt`와 `vt_candidates`가 `assets/visual-html-templates/`의 canonical name과 일치하는지 확인.
- [ ] `wg_candidates`가 `assets/widget-templates/`의 `wg-NN`과 일치하는지 확인.
- [ ] custom gate가 필요한 모드는 `custom_contracts`에 명시하되, custom gate 구현 자체는 기존 Python 함수에 남긴다. Registry는 적용 대상 선언만 담당한다.

### 자체 테스트
- [ ] 모든 Registry JSON이 `python3 -m json.tool` 통과.
- [ ] Registry mode 수 = 17.
- [ ] priority가 1~17 연속이고 중복 없음.
- [ ] mode id 중복 없음.
- [ ] layout/recipe/vt/wg 참조 파일 존재.

### 이슈 및 수정
- [ ] 발견 이슈 없음 또는 이 Phase에 기록 후 수정

### 완료 조건
- [ ] 17개 mode JSON 작성 완료
- [ ] 파일 참조 검증 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 2. Registry loader와 sync checker 구현 [B-lite]

### 목표
- Registry를 코드에서 안전하게 읽는 공통 모듈을 만든다.
- Registry와 기존 표면의 불일치를 잡는 검증기를 만든다(이 단계의 validator 대조는 **읽기 전용 비교**일 뿐, 동작을 바꾸지 않는다).

### 구현 태스크
- [ ] `skills/adaptive-html-final/scripts/mode_registry.py` 추가.
- [ ] `load_mode_registry(skill_dir)` 구현.
- [ ] `ModeDefinition` dataclass 또는 dict validation 구현.
- [ ] `get_mode_by_id`, `get_mode_by_layout_class`, `get_mode_by_priority` helper 구현.
- [ ] `skills/adaptive-html-final/scripts/check_mode_registry_sync.py` 추가.
- [ ] `manifest.json.modes`와 Registry 비교.
- [ ] `assets/layouts/*.html`와 Registry `layout_file` 비교.
- [ ] `recipes/*.prompt.md`와 Registry `recipe` 비교.
- [ ] `SKILL.md` marker block과 Registry 비교.
- [ ] `AGENTS.md` marker block과 Registry 비교.
- [ ] `validate_output.py`의 기존 `MODE_TEMPLATE_CONTRACTS`와 Registry 비교. Phase 3 완료 전까지는 byte-equivalent bridge로 유지한다.

### 자체 테스트
- [ ] `python3 skills/adaptive-html-final/scripts/check_mode_registry_sync.py --skill-dir skills/adaptive-html-final`
- [ ] sync checker가 불일치 fixture를 잡는지 단위 테스트 또는 governance fixture로 확인.
- [ ] 정상 상태에서 sync checker exit code 0.

### 이슈 및 수정
- [ ] 발견 이슈 없음 또는 이 Phase에 기록 후 수정

### 완료 조건
- [ ] Registry loader 동작
- [ ] sync checker가 manifest/SKILL/AGENTS/references/validator 표면을 검증
- [ ] 다음 Phase 진행 가능

---

## Phase 3. validator의 공통 mode contract Registry화 [B-full]

> **착수 전제:** B-lite(Phase 0·1·2·4) 완료 + 전체 검증 OK. 그리고 (a) 실제 18번째 모드 필요 또는 (b) 사용자 명시 지시. 그 전에는 착수하지 않는다.

### 목표
- `validate_output.py`의 공통 모드 계약을 Registry 기반으로 전환한다.
- 특수 의미 검증은 유지하되, layout/vt/wg/toc/depth 같은 공통 계약은 Registry에서 읽는다.
- **전환 범위를 고정한다:** `MODE_TEMPLATE_CONTRACTS`, `mode_template_contract_gate()`, `analysis_toc_map_required_gate()` allowlist, mode depth 기본 계약만 Registry화한다. YouTube/Manual/GitHub 의미 검증 함수 내부는 이번 Phase에서 일반화하지 않는다.

### 구현 태스크
- [ ] **deep-equal bridge 전제(P2-4):** registry-생성 `MODE_TEMPLATE_CONTRACTS`가 현행 하드코딩 dict와 **deep-equal임을 단위 테스트로 assert**한다. 이 테스트가 통과한 뒤에만 게이트를 registry 구동으로 전환한다(무회귀 증명).
- [ ] `MODE_TEMPLATE_CONTRACTS`를 Registry 기반 생성으로 대체(위 deep-equal 통과 후).
- [ ] `mode_template_contract_gate()`가 Registry의 `primary_vt`, `vt_candidates`, `wg_candidates`, `layout_class`를 사용하도록 수정.
- [ ] `analysis_toc_map_required_gate()`의 판정을 Registry `toc_contract.rule`로 보강하되 **현행 동적 로직을 보존**한다: `rule=="always"`→분석 모드 강제, `rule=="structural_h2_gte_4"`→`h2_count>=4`일 때만 강제. 정적 불리언으로 치환하지 않는다(P1-1).
- [ ] `mode_depth_gate()`의 하한을 Registry `quality_contract.mode_depth_min_avg`로 보강하되 기본값은 현재 `400` 유지.
- [ ] `profile_vt_required_gate()`가 Registry `primary_vt`, `vt_markers` 판정과 충돌하지 않도록 정리.
- [ ] custom gate 함수는 우선 유지: `github_analysis_visual_contract_gate`, `github_feature_usage_contract_gate`, `youtube_analysis_contract_gate`, `manual_analysis_contract_gate`.
- [ ] custom gate 적용 대상은 Registry `custom_contracts`로 선언하고 Python dispatch는 그 선언을 읽도록 보강. custom gate 함수명은 allowlisted mapping으로만 연결한다.

### 자체 테스트
- [ ] 기존 examples validate OK.
- [ ] Registry에서 primary vt를 틀리게 한 임시 fixture가 `mode_primary_vt_missing` 또는 sync fail을 발생.
- [ ] Registry에서 wg 후보를 틀리게 한 임시 fixture가 `mode_recommended_wg_missing` 또는 sync fail을 발생.
- [ ] custom gate 대상 모드의 기존 실패 fixture가 계속 실패.
- [ ] 비 custom 일반 모드의 기존 성공 fixture가 계속 성공.

### 이슈 및 수정
- [ ] 발견 이슈 없음 또는 이 Phase에 기록 후 수정

### 완료 조건
- [ ] 공통 mode contract가 Registry를 사용
- [ ] custom gate와 공통 gate 경계가 문서화됨
- [ ] 다음 Phase 진행 가능

---

## Phase 4. 문서/manifest/examples sync gate 추가 [B-lite] (Phase 3보다 먼저)

### 목표
- Registry와 문서 표면이 어긋나면 governance에서 실패하게 만든다.
- 문서를 자동 생성하기보다 우선 **검증 중심**으로 도입해 회귀 위험을 줄인다.
- **B-lite 마무리 단계.** 이 Phase까지 끝나면 validator를 건드리지 않고도 드리프트 방지 가치를 확보한다.

### 구현 태스크
- [ ] **마커 삽입(1회성, P2-2):** `SKILL.md`·`AGENTS.md`·`references/mode-selection.md`·`visual-html-system.md`·`widget-system.md`의 현행 결정표를 `<!-- AHF:MODE_TABLE_START/END -->`로 감싼다. **표 내용은 변경하지 않는다**(삽입만).
- [ ] **의미 비교 구현(P2-2):** sync checker가 marker block을 표로 파싱해 셀 값(id·layout·priority·primary vt·wg 후보)을 Registry와 대조한다. byte/문자열 일치 요구 금지.
- [ ] `manifest.json.modes`는 `id + layout` 중심의 최소 공개 메타로 유지하고, 상세 계약을 manifest에 확장하지 않는다. 상세 계약은 Registry만 정본이다(manifest.modes는 Registry의 파생 투영).
- [ ] `SKILL.md`의 `<!-- AHF:MODE_TABLE_START -->` marker block이 Registry와 일치하는지 sync checker에 포함.
- [ ] `AGENTS.md`의 `<!-- AHF:MODE_TABLE_START -->` marker block이 Registry와 일치하는지 sync checker에 포함.
- [ ] `references/mode-selection.md`의 marker block priority 목록이 Registry와 일치하는지 검증.
- [ ] `references/visual-html-system.md`의 marker block mode→primary vt 표가 Registry와 일치하는지 검증.
- [ ] `references/widget-system.md`의 marker block mode→wg 후보 표가 Registry와 일치하는지 검증.
- [ ] `examples/index.html`의 17개 링크가 Registry mode 수와 일치하는지 검증.
- [ ] `examples/sources/adaptive-html-final-manifest.json`과 `manifest.json` 동일성 유지.

### 자체 테스트
- [ ] `python3 skills/adaptive-html-final/scripts/check_mode_registry_sync.py --skill-dir skills/adaptive-html-final`
- [ ] `python3 skills/adaptive-html-final/tests/test_governance_gates.py`
- [ ] 임시로 `AGENTS.md` 결정표 한 항목을 틀리게 했을 때 sync checker가 실패하는지 로컬 확인 후 원복.
- [ ] 임시로 `SKILL.md` 결정표 한 항목을 틀리게 했을 때 sync checker가 실패하는지 로컬 확인 후 원복.

### 이슈 및 수정
- [ ] 발견 이슈 없음 또는 이 Phase에 기록 후 수정

### 완료 조건 (= B-lite 클로징)
- [ ] 문서/manifest/examples 주요 표면이 Registry와 동기화 검증됨
- [ ] **B-lite 신규 파일(`modes/*.json`, `scripts/mode_registry.py`, `scripts/check_mode_registry_sync.py`)을 `.skill`에 포함해 재패키징**(byte-match 확인)
- [ ] **Registry sync 테스트를 `test_governance_gates.py`에 추가하고 `manifest.quality.governance_count`를 실측과 동기화**
- [ ] 전체 검증 OK: V / G / C / Q / `git diff --check` / source manifest 동일성
- [ ] **여기까지가 B-lite. B-full(Phase 3·5·6·7)은 신규 모드 필요 또는 명시 지시 시 착수**

---

## Phase 5. governance·completion 통합 및 패키지 정합 [B-full]

> 참고: B-lite 단계(Phase 0·1·2·4)에서도 Registry sync 테스트를 `test_governance_gates.py`에 추가하고 `manifest.quality.governance_count`를 재측정·동기화한다. 본 Phase는 B-full 전환(Phase 3) 이후의 최종 통합·패키지 정합을 다룬다.

### 목표
- Registry sync가 최종 완료 게이트에 포함되도록 한다.
- `.skill` 패키지와 워킹트리 정합을 유지한다.

### 구현 태스크
- [ ] `test_governance_gates.py`에 Registry sync 테스트 추가.
- [ ] Registry sync는 `test_governance_gates.py`에 포함한다. `completion_check.py`는 기존 완료 단계 수를 늘리지 않고, governance 실행 결과를 통해 간접 포함한다.
- [ ] completion 출력의 기존 governance 단계에서 Registry sync 실패 원인이 드러나도록 에러 메시지를 명확히 한다.
- [ ] `manifest.quality.governance_count`를 실측과 동기화한다.
- [ ] `.pytest_cache/`, `__pycache__/`, `.DS_Store` 같은 환경 노이즈 제외 규칙 유지.
- [ ] 소스 변경이 완료되면 `skills/adaptive-html-final.skill` 재패키징.
- [ ] `.skill` 내부 파일이 워킹트리와 byte-match하는지 확인.

### 자체 테스트
- [ ] `python3 skills/adaptive-html-final/scripts/validate_output.py skills/adaptive-html-final/examples --skill-dir skills/adaptive-html-final`
- [ ] `python3 skills/adaptive-html-final/scripts/quality_contract_check.py skills/adaptive-html-final/examples`
- [ ] `python3 skills/adaptive-html-final/tests/test_governance_gates.py`
- [ ] `python3 skills/adaptive-html-final/scripts/completion_check.py skills/adaptive-html-final/examples`
- [ ] package byte-match 검사. 기존 검사 스크립트가 없으면 `scripts/package_skill.py`와 `scripts/verify_skill_package.py`를 이번 범위에 추가할지 명시적으로 결정하고, 추가 시 `.pytest_cache/`, `__pycache__/`, `*.pyc`, `.DS_Store` 제외 규칙을 공유한다.
- [ ] `git diff --check`

### 이슈 및 수정
- [ ] 발견 이슈 없음 또는 이 Phase에 기록 후 수정

### 완료 조건
- [ ] governance count 실측과 manifest 일치
- [ ] `.skill` 패키지 정합 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 6. 신규 모드 추가 runbook 검증 [B-full]

### 목표
- 실제 18번째 모드를 추가하지 않고도, 신규 모드 추가 절차가 명확하고 검증 가능함을 증명한다.
- “모드 추가가 쉬워졌다”를 문서가 아니라 절차로 확인한다.

### 구현 태스크
- [ ] `docs/adaptive-html-final-add-mode-runbook.md` 작성.
- [ ] 신규 모드 추가 절차를 10단계 이하로 정리.
- [ ] 필요한 파일 목록 명시: mode JSON, layout, recipe, optional reference, optional custom gate, example.
- [ ] `custom_contracts`가 필요한 경우와 필요 없는 경우를 구분.
- [ ] `tests/fixtures/mode-registry/` 아래 dry-run fixture로 “추가 모드 누락 시 sync fail”을 확인.
- [ ] 실제 18번째 모드 파일은 커밋하지 않는다. 필요 시 `tests/fixtures/`의 비실행 fixture로만 둔다.

### 자체 테스트
- [ ] runbook 절차만 보고 신규 모드 추가자가 수정해야 할 파일을 알 수 있음.
- [ ] `tests/fixtures/mode-registry/missing-layout`, `missing-recipe`, `duplicate-priority`, `stale-skill-table`, `stale-agents-table`, `invalid-vt-marker`, `invalid-wg-marker` 중 최소 5종이 실패함.
- [ ] 기존 17모드 검증은 계속 OK.

### 이슈 및 수정
- [ ] 발견 이슈 없음 또는 이 Phase에 기록 후 수정

### 완료 조건
- [ ] 신규 모드 추가 runbook 작성 완료
- [ ] dry-run 검증 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 7. 최종 문서·검증·리스크 정리 [B-full]

### 목표
- B안 개편 결과를 문서화하고, 남은 리스크를 명확히 남긴다.
- 버전 승격 없이 v5.10.3 정합 상태로 마무리한다.

### 구현 태스크
- [ ] `SKILL.md`에 Registry가 mode 정의 단일 출처임을 반영.
- [ ] `AGENTS.md`에 Registry sync 원칙 반영.
- [ ] `README.md`에 “모드 추가는 Registry 기반” 한 줄 가이드 추가 여부 검토.
- [ ] `CHANGELOG.md`에는 승인 전이면 `Unreleased`로 기록.
- [ ] 버전 표면에 `5.10.4`가 생기지 않았는지 검색.
- [ ] `output/`은 이번 작업 정본에서 제외했음을 명시.

### 자체 테스트
- [ ] `rg -n "5\.10\.4|v5\.10\.4" AGENTS.md README.md Guide.md skills/adaptive-html-final docs dev-plan --glob '!*.skill'` 결과가 의도된 문맥 외 0.
- [ ] `python3 skills/adaptive-html-final/scripts/check_mode_registry_sync.py --skill-dir skills/adaptive-html-final`
- [ ] `python3 skills/adaptive-html-final/scripts/validate_output.py skills/adaptive-html-final/examples --skill-dir skills/adaptive-html-final`
- [ ] `python3 skills/adaptive-html-final/scripts/quality_contract_check.py skills/adaptive-html-final/examples`
- [ ] `python3 skills/adaptive-html-final/tests/test_governance_gates.py`
- [ ] `python3 skills/adaptive-html-final/scripts/completion_check.py skills/adaptive-html-final/examples`
- [ ] `git diff --check`

### 이슈 및 수정
- [ ] 발견 이슈 없음 또는 이 Phase에 기록 후 수정

### 완료 조건
- [ ] B안 모듈화 완료
- [ ] 17개 기존 모드 무회귀 검증 완료
- [ ] 신규 모드 추가 절차 문서화 완료
- [ ] 버전 `5.10.3` 유지 확인

---

## 최종 완료 기준

- [ ] `skills/adaptive-html-final/modes/`에 17개 mode definition 존재.
- [ ] Registry loader가 단일 출처로 동작하고, 정본 경로가 `skills/adaptive-html-final/modes/NN-<mode>.json`으로 고정됨.
- [ ] `manifest.json.modes`, `SKILL.md` marker block, `AGENTS.md` marker block, references marker block, `validate_output.py` 공통 계약이 Registry와 불일치하면 실패.
- [ ] 기존 17개 examples validate/quality/completion OK.
- [ ] governance gate 전체 OK.
- [ ] `.skill` 패키지 byte-match OK.
- [ ] 신규 모드 추가 runbook 존재.
- [ ] 승인 없는 버전 상승 없음.

## 잔여 리스크 / 후속 과제

| 리스크 | 설명 | 대응 |
|---|---|---|
| 문서 자동 생성 범위 과다 | SKILL/AGENTS 전체를 자동 생성하면 가독성·역사 설명이 깨질 수 있음 | 1차는 검증 중심, 자동 생성은 표 구간만 제한 |
| custom gate 과도한 일반화 | YouTube/Manual/GitHub 특수 의미 검증을 무리하게 JSON화하면 품질 저하 | custom gate는 유지하고 적용 대상만 Registry로 선언 |
| validator 회귀 | `validate_output.py`가 큰 파일이라 수정 리스크 높음 | Phase 3에서 공통 gate만 작게 전환, 기존 fixture 유지 |
| 버전 표면 드리프트 | 구조 개편 중 버전 bump 유혹 | v5.10.3 유지, release guide 기준 적용 |
| 신규 모드 실제 추가 미검증 | runbook만으로 충분한지 불확실 | dry-run fixture와 sync fail 테스트로 절차 검증 |

