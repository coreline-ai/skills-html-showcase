# implement_20260611_152118.md

작성 일시: `2026-06-11 15:21 KST`

이 문서는 v5.10.2 직후 2차 전문가 리뷰에서 확인된 발견을 다음 개발 차수(v5.10.3 또는 v5.11.0)의 범위로 고정하기 위한 작업 문서다. 모든 발견은 2026-06-11 실측(렌더 측정·zip 검사·전수 grep) 근거가 있으며, 구현이 목적 밖으로 확장되지 않도록 항목별 완료 기준을 명시한다.

## 개발 목적

v5.10.2에서 닫은 결함 클래스(폭 누락·버전 드리프트)와 같은 패턴의 잔존 결함 4건을 해소하고, 각 결함을 재발 불가능하게 게이트로 성문화한다.

## 배경 (2차 리뷰 발견 요약)

| 우선순위 | 발견 | 근거 |
|---|---|---|
| P1 | 배포 패키지 `skills/adaptive-html-final.skill`이 **v5.7.0·16모드**로 동결(현행 5.10.2·17모드). Guide.md:37이 "설치용 패키지"로 안내 중 → 설치 사용자는 3개 버전 전 스킬을 받음. 재패키징 절차·버전 일치 게이트 부재 | zip 내 manifest 직접 확인 |
| P2-A | **골격↔예제 폭 클래스 드리프트 5건**: beginner/article/blog/education/case 골격은 `page`(narrow), 정본 예제는 `page-wide`. `layout-case`는 60rem 목록·R5에도 포함되어 3중 모순. 예제 01·03·04·05는 page-wide인데 60rem 미포함 → 단락 46rem/컨테이너 1020px 비대칭(= v5.10.2에서 고친 github-feature와 동일 증상) 잠복. layout-system.md에 폭 규정 없음, 골격↔예제 일치 게이트 없음 | 17레이아웃 × 18예제 전수 대조 |
| P2-B | 테마 스위처 게이트가 8테마 중 3개만 강제(light/white/dark + light2↔dark2 페어). blue·skyblue·sepia 무검사 → "8-테마 단일 계약" 선언 대비 게이트 3/8 | validate_output.py:379-396 |
| P2-C | 저장소 비대화 지속: output/ 추적 1473파일, .git 400M. 정책 부재 | git ls-files 실측 |
| P3 | widget-system.md:141 dangling `output/showcase-v5` 링크 · Guide.md:277 `--profile` 필수처럼 표기 · `orginal_skill/` 오타 · exporter 스크립트 2중 사본 동기화 게이트 없음(현재 sha 동일) | grep/shasum |

## 개발 범위

1. **P1 — .skill 재패키징 + 패키지 게이트**
   - 현행 `skills/adaptive-html-final/` 디렉터리를 zip으로 재패키징(`__pycache__`·`.DS_Store` 제외).
   - `skill_doc_consistency_gate`에 추가: 형제 경로에 `adaptive-html-final.skill`이 존재하면 zip 내 `manifest.json` version이 현행 manifest.version과 일치해야 한다(불일치 = `skill_package_version_stale`).
   - 재패키징 절차 1줄을 AGENTS.md 또는 skills/adaptive-html-final/README.md에 명시.
2. **P2-A — 폭 정본 결정 및 정렬(설계 결정 필요)**
   - 결정안 A(권고): 예제=정본. 골격 5종(beginner-learning/magazine-article/personal-blog-essay/course-module/case-study)을 `page-wide`로 승격하고, beginner/article/blog/education 4종을 theme.css 60rem 목록과 R5 regex에 편입. 모든 모드 = wide + 960px 단락으로 통일.
   - 결정안 B: 골격=정본(읽기형 4모드는 narrow 의도). 예제 01/03/04/05/11을 `page`로 강등, layout-case를 60rem 목록·R5에서 제거.
   - 채택안과 무관하게: `references/layout-system.md`에 모드별 page/page-wide 표 성문화 + **골격↔예제 main class 일치 게이트** 신설(거버넌스 테스트 포함).
   - 결정안 A 채택 시 theme.css 변경 = 코어 해시 갱신 → examples 18종 재인라인 + sources 동기화 + 버전 bump 절차(메모리의 cascade 체크리스트) 전체 수행.
3. **P2-B — 테마 게이트 8/8 확장**
   - `theme_switcher_contract_gate`: 8개 라디오(`ahf-light/light2/white/dark/dark2/blue/skyblue/sepia`) + 라벨 전부 검사. 단계적 확장 테마 허용 정책을 유지할 경우 "코어 3 + 확장 5는 전부-또는-전무" 규칙으로 명문화.
4. **P2-C — output/ 정책 결정(별도 차수 가능)**
   - 후보: (a) 신규 산출물 커밋 중단 + 기존은 유지, (b) 아카이브 브랜치 분리, (c) Git LFS. GitHub Pages 서빙 의존(README 라이브 갤러리 링크)을 고려해 결정.
5. **P3 일괄**: widget-system.md 링크를 `examples/`로 교체, Guide.md `--profile`을 "(선택, sources/profile.json 자동 인지)"로 정정, exporter 사본 sha 일치 검사(선택).

## 개발 제외 (이번 차수 아님)

- 신규 모드/레이아웃/위젯 추가 없음. 콘텐츠 재생성 없음(P2-A 결정안 A의 main class 치환·재인라인 제외).
- `--max-wide`(1020px) 컨테이너 폭 변경 없음.

## 완료 기준

- [x] zip 내 manifest.version == 현행 version(5.10.3 재패키징), `skill_package_version_stale` 게이트+거버넌스 2종.
- [x] 17레이아웃 × 18예제 main class 전수 일치(골격 5종 page-wide 승격), layout-system.md 폭 정본 성문화, `layout_width_consistency_issues` 게이트.
- [x] 결정안 A 채택 — 예제 01/03/04/05 단락 실측 736→960px(05는 article>section 셀렉터 포함).
- [x] 테마 스위처 게이트 8/8 강제(3/5/6-라디오 픽스처는 flag 기대로 전환).
- [x] governance 113/113 + validate(examples 18) OK + completion_check 3/3.
- [x] v5.10.3 cascade 전부 수행(코어 해시 c3e0e4e4…, 4자산 18종 재인라인, 프로즈 전체).

---

## 추가: 3차 전문가 리뷰 발견 (2026-06-11 오후, 동일 차수 백로그)

3개 병렬 감사(CSS 시스템 내부 / 자산 템플릿 46종 / 스크립트·스키마·테스트) + 렌더 실측 검증 결과. 2차까지의 발견과 중복 없음.

### P1 (렌더 실측으로 확정)

| # | 발견 | 실측 근거 | 수정 방향 |
|---|---|---|---|
| 3-P1-A | **widgets.css "카탈로그 reverse-sync" 패치층(846–973)의 하드코딩 `color:#fff`** — `.wg-02-pv-cta/.wg-06-btn--primary/.wg-07-pill/.wg-09-next/.wg-20-chip/.wg-02-badge/.wg-17-h3-no/.wg-01-rank-pick/.wg-01-pick-badge`(852–857)가 `var(--accent-2)` 배경과 결합. 다크 트리오에서 `--accent-2`는 밝은 틴트로 뒤집히므로 흰 글자가 안 보임 | ex10 dark/dark2/blue 렌더: fg=white, bg=rgb(255,144,154)/(217,160,165)/(146,189,255), **대비 1.92** (AA 4.5 미달) | `#fff` → 토큰 페어링(`--accent`+`--on-accent` 또는 `--on-accent-2` 신설). 동일 층의 `#5f88a2`·`#9b6a20`·`#fff3c4`(wg-20-var)도 토큰화 |
| 3-P1-B | **인쇄 시 `.try` 본문 텍스트 소실** — print.css:5가 `.try{background:#f4f4f4;color:#111}`로 바꾸지만 components.css:112 `.try p/li{color:#d0d0c8}`가 특이도로 승리 | print 에뮬레이션: p/li fg=rgb(208,208,200) on rgb(244,244,244) ≈ **1.3:1** | print.css에 `.try p,.try li,.try a,.try .label{color:#111}` 추가. 함께: `details` 강제 펼침, `@media print` 다크 토큰 리셋(`:root` 라이트 강제), `a::after` href 출력을 `a[href^="http"]`로 한정 |

### P2

- **3-P2-1 wg-07 애니메이션 사망**: widgets.css:944 `.wg-07-box{animation:none}`이 모든 변형·reduced-motion 블록을 무력화. 879행 주석은 "미반영"이라 명시 — 코드와 모순. 복원 또는 컨트롤 제거 결정 필요. (wg-09 scroll-snap 제거로 인한 정체성 드리프트도 동반 정리)
- **3-P2-2 `--danger-accent` 다크 누락**: theme-dark.css `#ahf-dark` 블록에 없음 → 다크에서 RISK/TROUBLE 좌측 라인 거의 안 보임(dark2/blue는 있음).
- **3-P2-3 forced-colors/prefers-contrast 전무**: CSS-only 컨트롤(테마바·탭·칩)이 배경색만으로 상태 전달 → Windows 고대비에서 상태 소실. 최소 테마바·탭에 보더/언더라인 상태 단서 추가.
- **3-P2-4 수동 체크리스트 부패**: tests/layout-checklist.md "14개"+고아 행+모드17 부재(구조적 부패), quality/widget/visual-regression-checklist 3종도 모드17 누락. accessibility/golden-prompts만 최신.
- **3-P2-5 exporter `--require-webp` 오판**: export_output.mjs:908이 `skipped`(예: `no_dom_radio` — 정상 케이스)도 실패 처리. `sharp_unavailable`/render 실패만 실패로.
- **3-P2-6 렌더러 제목 겹침**: render_visual_svg.py:169–174 — 제목 2줄 시 부제(y=1280)와 겹침. maxLength 80 허용과 모순. 줄바꿈 시 부제 y 오프셋 또는 ellipsis.
- **3-P2-7 거버넌스 미커버 게이트 7종**: widget_static/visual_html/cross_leak/mode_template_contract/direct_section_title_icon/body_icon_diversity/analysis_toc_map — 회귀 잠금 없음.
- **3-P2-8 위젯 h2 강등 규칙 미문서화**: wg 17종이 자체 h2 보유 — 섹션 내부 삽입 시 h3 강등 규칙을 widget-system.md에 1문장 성문화(+wg-08/10/13의 h3-top 비일관 정리).
- **3-P2-9 editorial 패턴 03/05 메타 placeholder**: "핵심 명제 한 문장을 여기에" 류 — 출력 누출 위험. 실데모 콘텐츠로 교체.

### P3 (배치 정리)

`.score` 비스코프 셀렉터(visual-html.css:24) · vt-10 `→` 화살표 aria-hidden 누락 · sepia 하드코딩 흰색 군(다크 트리오 패치층 미적용) · text-bullet-view 다크 패치 #ahf-dark 한정 · visual-html.css:3 `body{background}` 전역 부작용 · quality-report.schema.json 고아(참조 0) · wg-14 focus 시 탭 3개 전부 outline · 860–980 브레이크포인트 6종 통합 후보 · demo/(2M)+templates/(2.3M)+docs/(13M) 레거시 17MB — AGENTS/SKILL 참조 0, P2-C output 정책과 함께 결정.

### 3차 검증 그린 (수정 불요 확인)

8테마 body bg 8/8 구분 정상 · 자산 계약 전수 일치(카탈로그↔파일 46종, base.html 17슬롯, SVG title/desc 46/46, 위젯 입력 라벨 0건 누락, 네임스페이스 순수) · 스크립트 실행 검증(렌더러 7템플릿 렌더 성공, completion_check 오케스트레이션 건전, exporter 1041줄 고품질·읽기전용 증명) · 예제 gzip 평균 50KB(인라인 CSS 90%는 결정론 설계 비용으로 수용).

---

## 4차: 회귀 조건 리뷰 — 패치 안전성 설계 (2026-06-11 저녁)

"잘못 패치하면 전체 레이아웃이 깨진다"는 위험을 수정안별로 분해해, **폭발 반경·불변 조건·측정 방법·시퀀스**를 사전 확정한다. 아래 사실은 전부 실측이다.

### 4.1 수정안의 위험 등급 분류 (자산 클래스 기준)

| 등급 | 대상 | cascade 요구 | 시각 위험 |
|---|---|---|---|
| **A (코어 5종)** | print.css 수정 / (P2-A 결정안 A 시) theme.css 60rem | 코어 해시 갱신 → examples 18종 재인라인 + sources + 버전 bump 전체 | print.css: **0 — 전 규칙이 `@media print` 내부임을 파싱으로 확정**(스크린 무영향 구조 보장). theme.css: 의도적 폭 변화만 |
| **B (조건부 자산)** | widgets.css(#fff 스윕·wg-07) / theme-dark.css(--danger-accent) | 코어 해시 **불변**(24d2e3b4 유지). 단 examples에 인라인된 해당 CSS 재주입 + sources/assets 스냅샷 + css-integrity asset_sha256 갱신 | 라이트 0 (아래 4.2 수치 증명), 다크 의도 개선만 |
| **C (검증기·문서·도구)** | 게이트 신설, 테마 게이트 8/8, 체크리스트, exporter, renderer, .skill repack | 없음 (단 게이트 강화가 기존 통과 디렉터리를 깨지 않는지 사전 dry-run) | 렌더 위험 0 |

### 4.2 P1-A의 회귀 안전 설계 — `#fff` → `var(--on-accent)` (수치 증명)

테마별 (--accent-2 bg, 제안 잉크) 대비비 실측:

| 테마 | 현행 #fff | 제안 var(--on-accent) | 라이트 시각 변화 |
|---|---|---|---|
| light/light2/white | 6.09 OK | 6.09 | **없음 — on-accent=#ffffff라 byte-identical** |
| skyblue | 7.72 OK | 7.72 | **없음** |
| sepia | 7.66 OK | 7.10 OK | #fff→크림(#fbf6e9) 미세, sepia 설계와 정합 |
| dark | **2.16 FAIL** | **8.98 OK** | 의도 개선 |
| dark2 | **2.21 FAIL** | **8.62 OK** | 의도 개선 |
| blue | **1.92 FAIL** | **9.68 OK** | 의도 개선 |

→ 신규 토큰 불필요. 라이트 4테마는 **수학적으로 변화 0**. 폴백 문법 전례 존재(`var(--on-accent,#fff)` 기사용). **이번 차수 제외 권고**: wg-13-node--decide(#9b6a20+#fff=4.69, 전 테마 AA 통과)와 wg-20-var(자체 대비 7.10 OK) — 위험 대비 이득 낮음, P3 유지.

### 4.3 영향 마크업 센서스 (유한 프로브 목록)

영향 클래스가 실제 쓰인 예제: **ex10**(wg-01-pick-badge/rank-pick) · **ex07**(wg-02-pv-cta/badge) · **ex09**(wg-20-chip) · **ex05**(wg-17-h3-no). wg-06-btn--primary/wg-07-pill/wg-09-next는 예제 마크업 0(미래 출력만 영향). → 시각 검증 대상이 **4개 예제 × 특정 셀렉터**로 한정된다.

### 4.4 회귀 불변 조건 (전 패치 공통 INVARIANTS)

- **I1** 라이트(기본) 테마에서 18예제 렌더 동일 — 4.5의 의도 변화 목록(D)만 예외.
- **I2** 해시 규율: B만 적용 시 코어 해시 `24d2e3b4…` 불변. A 포함 시 신해시 + cascade 전체(메모리 체크리스트: manifest·sources·CHANGELOG·SKILL/AGENTS/README/Guide/examples kicker).
- **I3** 390/768/1280 가로 오버플로 0 (기존 스캐너 재사용).
- **I4** 8테마 body bg 8/8 구분 + light 기본 checked.
- **I5** governance(≥88)·validate(examples OK)·completion_check 3/3·quality_contract OK.
- **I6** 무 JS·무 embed·h1=1·테마바 계약 불변.

### 4.5 의도 변화 목록 (EXPECTED DELTAS — 이것 외 변화는 회귀)

- **D1** 다크 트리오: 7개 클래스 잉크 흰→다크(대비 ≥8.6).
- **D2** sepia: 동일 클래스 잉크 #fff→#fbf6e9.
- **D3** print: .try p/li/a/.label = #111(대비 ≥12) · details 펼침 · 내부 앵커 ::after 미출력 · 다크 체크 상태 인쇄 시 라이트 토큰 강제.
- **D4** (--danger-accent 추가 시) 다크에서 RISK/TROUBLE 좌측 라인 가시화.
- **D5** (wg-07 결정에 따라) 애니메이션 복원 또는 컨트롤 제거 — 라이트 포함 의도 변화, 별도 캡쳐.
- **D6** (P2-A 결정안 A 시) 예제 01/03/04/05 단락 736→960px + 골격 5종 page-wide.

### 4.6 프로브 기반 측정 런북 (pre/post)

1. **Step 0 베이스라인**: 패치 전 채집 → `dev-plan/baseline_v5102_probes.json`
   - 토큰 프로브: {ex10·07·09·05 × 영향 셀렉터} × 8테마 × computed{bg,color}+대비비 (Playwright, checked 프로퍼티 주입 방식)
   - 구조 프로브: 18예제 × {1280,390} 풀페이지 스크린샷(light) + scrollWidth/clientWidth
   - print 프로브: ex02 .try {sec,p,li,a} computed 색
2. **패치 적용 단위마다**: 동일 프로브 재실행 → diff가 **정확히 D-목록과 일치**해야 통과(그 외 1건이라도 변화 = 중단·원인 분석).
3. **픽셀 diff**(보조): 대표 8예제 light 1280 풀페이지 before/after — 의도 변화 구역 외 0픽셀.

### 4.7 신설 회귀 게이트 (재발 차단)

- **G1 `on_accent_pairing_violation`** (asset lint, skill_asset_lint 확장): widgets/visual-html/editorial-patterns에서 `var(--accent…)` 배경 선언 블록 내 `color:#fff` 하드코딩 금지 → 현행 위반 6곳이 잡히는 자기검증 후 도입.
- **G2 print 잉크 게이트**: print.css에 `.try p` 잉크 오버라이드 존재 + (출력 검사) 인라인 print 영역에 해당 규칙 포함.
- **G3 토큰 대비 게이트**(거버넌스): theme-dark.css를 파싱해 테마별 (accent-2, on-accent) 대비 ≥4.5 정적 검증 — 브라우저 불필요(순수 산식).

### 4.8 패치 시퀀스 (체크포인트식 — 각 단계 그린 아니면 중단)

```text
S0 베이스라인 채집(4.6) → 커밋 전 보관
S1 Class C 일괄(게이트·문서·도구·.skill) → 게이트 dry-run(기존 OK 디렉터리 비파괴 확인)
S2 Class B(widgets #fff 스윕 + theme-dark --danger-accent [+ wg-07 결정]) 
   → 조건부 재인라인(18예제 widgets.css/theme-dark.css 재주입 + 스냅샷 + css-integrity)
   → 프로브 diff = D1/D2/D4만 → I1~I6 전부 그린
S3 Class A(print.css 6줄 [+ P2-A 결정 시 theme.css]) 
   → 코어 해시 cascade 전체 + 버전 v5.10.3 bump(프로즈 포함)
   → 프로브 diff = D3[/D6]만 → 전체 게이트 + 1280/390 캡쳐
S4 단일 커밋(또는 S2/S3 분리 커밋) + 푸시
```

**결정 대기**: ① wg-07 복원 vs 컨트롤 제거, ② P2-A 결정안 A/B(폭 정본), ③ P2-A를 S3에 동승할지 별도 차수로 뺄지.

---

## 5차: 실행 기록 — v5.10.3 반영 완료 (2026-06-12)

### 채택 결정 (사용자 위임 → 권장안)

1. **wg-07 = 복원** — `animation:none` 정적화 제거(879행 카탈로그 메모 "미반영"과 코드 일치화). prefers-reduced-motion 블록이 접근성 보장.
2. **폭 정본 = 결정안 A** — 전 17모드 `.page-wide` + 단락 60rem. 골격 5종 승격, theme.css에 4모드×(section·article>section) 24셀렉터, R5 +4.
3. **P2-A 동승** — 단일 v5.10.3 cascade로 통합(코어 해시 1회 갱신).

### S0–S4 실행 결과

| 단계 | 결과 |
|---|---|
| S0 베이스라인 | `baseline_v5102_probes.json` — 토큰 6프로브×8테마, 폭 5, print, 18예제×2뷰포트 오버플로, 8테마 구분 |
| S1 자산 | widgets(#fff→on-accent 3규칙·wg-07 복원)·theme-dark(--danger-accent dark + 다크 트리오 인쇄 라이트 토큰 강제)·print(.try 잉크·details ::details-content·href http 한정·themebar 숨김)·theme(60rem+24)·골격 5종 wide |
| S1b 게이트 | 6종 신설(on-accent lint·theme_token_contrast·print_try_ink·layout_width_consistency·테마 8/8·skill_package_version) + R5+4 + 미커버 7종 거버넌스 잠금 → **88→113 checks** |
| S1c 도구·문서 | exporter --require-webp(skipped=sharp_unavailable만 실패, 루트 사본 sha 동기)·렌더러 2줄 제목(1줄 byte-동일)·체크리스트 4종 모드17(layout-checklist "14개"·고아행 수리)·widget-system h2 강등 규칙+갤러리 링크·Guide --profile 선택 표기·editorial 03/05 실콘텐츠·layout-system 폭 정본 |
| S2 cascade | 코어 해시 `24d2e3b4…→c3e0e4e4…`, 4자산×18예제 재인라인(전수 18/18), sources/css-integrity, manifest+CHANGELOG+프로즈(SKILL/AGENTS/README/Guide/skills-README/ex17 kicker) v5.10.3, **.skill 재패키징(5.7.0→5.10.3, 232entries)** |
| S3 프로브 diff | **불변 39건 확인 + 의도 변화 19건 = D1~D6 정확 일치, 진성 위반 0** (`post_v5103_probes.json`) |

### S3 위반 조사 기록 (전부 비회귀 판정)

- wg20chip 7건 "예상외 변경" → **프로브 아티팩트**: `.wg-20-chip{transition:all .15s}` + 테마 전환 90ms 후 샘플링이 전이 중간값 캡처. 700ms 안정화 후 2회 측정 동일 + 토큰 정확값(light accent+white / dark accent+on-accent / sepia accent+cream) 확인. 미체크 칩 규칙은 소스 무변경.
- details 펼침 측정 실패 1건 → 프로브 셀렉터 결함(ex02는 DOM details 0개 — 과거 grep이 CSS 주석 매칭). 실예제 ex01(6개)에서 `checkVisibility()`로 재검증: **screen 닫힘=false / print=true ✅**.
- **프로토콜 교훈**: 토큰 프로브는 transition 보유 컴포넌트에 대해 **테마 전환 후 ≥700ms 안정화** 필수(§4.6 런북에 반영).

### 잔여(차기 백로그로 이월)

P2-C output/ 정책 · 3-P2-3 forced-colors · wg-09 정체성 · wg-13/wg-20-var 토큰화(의도 제외) · sepia 하드코딩 군 · 3-P2-7 일부 P3들(.score 스코프, vt-10 화살표 aria, visual-html body bg, quality-report 스키마, 브레이크포인트 통합) · orginal_skill 오타 · 레거시 17MB.
