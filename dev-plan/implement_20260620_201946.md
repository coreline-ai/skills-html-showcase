# implement_20260620_201946.md

작성 일시: `2026-06-20 20:19:46 KST`
갱신 일시: `2026-06-21 KST` — **⚠️ SUPERSEDED(부분): mode 18·v5.11.0 주장 폐기. strategy-os는 slot 21+ 보류.**

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

> ### ⚠️ SUPERSEDED 배너 (2026-06-21 결정)
> 본 문서가 주장하던 **mode 18 / priority 18 / v5.11.0 귀속은 폐기**되었다.
> - **mode 18 = `business_plan_html`** 로 재배정([implement_20260621_150500.md](implement_20260621_150500.md)), 그리고 **버전 업 없이 `5.10.6`에 병합**(사용자 결정 2026-06-21).
> - **strategy-os는 보류** — 소스 `company_ai_strategy_dashboard.html`이 리포에 커밋되지 않았다(Phase 0 충실도 확인 불가). 소스 커밋 후 **slot 21+** 로 재검토하며, **mode 18도 v5.11.0도 아니다**.
> - 아래 본문의 "모드 18 / priority 18 / D4=5.11.0" 표기는 **역사 기록**으로만 둔다(재배정 전 계획). 실제 위젯 8종·무-JS 시나리오 스위처·전략-OS 프레임워크 설계는 strategy-os 재개 시 유효한 참고자료.

## 개발 목적

첨부 JS 인터랙티브 대시보드 `company_ai_strategy_dashboard.html`("회사 AI 전략 운영체계 — 흔들이 부서별 관점 대시보드")에서 재사용 가치가 높은 **시각 컴포넌트 8종**과 **콘텐츠 프레임워크(전략-OS 6단계 방법론)**를 분리해, `adaptive-html-final` 스킬에 **무-JS**로 통합한다. 원본의 시나리오 시뮬레이터(JS 재렌더)는 기존 8테마 스위처와 동일한 radio `:checked` 패턴으로 무-JS 재현한다.

최종 산출은 (1) 신규 위젯 `wg-21~wg-28`, (2) 신규 레이아웃 `strategy-os` + 신규 모드 `18`, (3) 무-JS 시나리오 스위처, (4) 콘텐츠 프레임워크를 담은 recipe/required_blocks, (5) V/Q/C를 통과하는 검증 예제다.

## 개발 범위

- **①구조/크롬**: 히어로 헤더·스티키 칩 내비·번호 원형 배지 섹션헤더 → 기존 크롬 자산과 중복 매핑 후 부족분만 보강.
- **②시각 컴포넌트 8종** → `widgets.css`에 `wg-21~wg-28` 추가(조건부 자산, 코어해시 불변):
  - wg-21 North Star 다크 패널 + goal 그리드(라디얼 글로우)
  - wg-22 파이프라인 스테이지 플로우(가로 N단계, 상단 컬러보더, `→` 화살표 커넥터, 칩)
  - wg-23 부서-역할 매트릭스(주관/협업/참고 배지 + dim/active 상태)
  - wg-24 수직 번호 스텝(counter 기반 커넥터 라인)
  - wg-25 스코어카드(라벨+진행바+값+병목 게이트노트)
  - wg-26 의사결정 4-카드(컬러 마크바)
  - wg-27 운영 리듬 4-카드(Daily/Weekly/Monthly/Quarterly)
  - wg-28 추천 다크 미니카드 + 긴급도 핀(high/med/low 상태색)
- **③인터랙션**: 시나리오 시뮬레이터 → radio `name="ahf-scenario"` + `:checked ~` 형제 선택자로 시나리오별 패널 토글(무-JS).
- **④콘텐츠 프레임워크**: North Star→신호 5종→10부서 관점→실행 워크플로→결정 4종→운영 리듬 4종 → 모드 `required_blocks` + `recipes/strategy-os.prompt.md` + 예제 콘텐츠로 매핑.
- 신규 모드 18(`strategy_os`): `layout-strategy-os` + `assets/layouts/strategy-os.html` + primary_vt + wg_candidates(신규 위젯).
- 검증 예제 `examples/18_strategy_os_*.html`(CSS 바이트-verbatim 재인라인 + `core-css-sha256` 마커 + 8테마 + 무-JS 시나리오).

## 제외 범위

- 원본의 JavaScript 로직 이식(스킬은 무-JS 원칙). `<script>`는 산출에 포함하지 않는다.
- 코어 CSS 5종(`theme/components/visual-components/layouts/print`) 직접 수정. 불가피하면 별도 결정 게이트(D2)에서 재검토하되 기본은 회피.
- 외부 폰트(Pretendard) 의존. 시스템 폰트 스택으로 대체.
- `output/` 산출물 정본 승격(독립 테스트 결과물이며 스킬 입력이 아니다).
- 원본 대시보드 자체를 `output/`에 "복원본"으로 만드는 작업(이번 범위는 패턴 분리·통합이지 원본 재현이 아님). 단 Phase 0에서 시각 충실도 레퍼런스용 1회 렌더는 허용.

## 참조 문서

- [AGENTS.md](../AGENTS.md) §2.1 머지 보호, §2.2 성능
- [docs/adaptive-html-final-template-authoring-protocol.md](../docs/adaptive-html-final-template-authoring-protocol.md)
- [skills/adaptive-html-final/manifest.json](../skills/adaptive-html-final/manifest.json) — 버전·governance_count 단일 출처
- [dev-plan/implement_20260618_221706.md](implement_20260618_221706.md) — 직전 하드닝 계획(게이트 신설·악성 fixture 절차 참고)
- [dev-plan/release-approval-v5.10.5.md](release-approval-v5.10.5.md) — 버전 승격 승인 문서 양식
- 원본: `company_ai_strategy_dashboard.html`(사용자 첨부, UTF-8/한국어)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- **코드 변경·커밋은 사용자 명시 요청 시에만** 한다(현 워크스트림 규칙).
- 신규 게이트는 악성 fixture로 실제 실패를 먼저 확인한 뒤 통과로 전환한다.

## 문서 기반 제약 사항

- 코어해시 자산 = 정확히 5종(`validate_output.py` `asset_order`): `theme.css, components.css, visual-components.css, layouts.css, print.css`. `widgets.css`·`visual-html.css`는 **조건부 자산**이라 변경해도 코어해시는 불변(단 `css-integrity.json` 전체 자산 해시는 갱신 필요).
- `widget_static_gate`: `wg-` 클래스 사용 시 widgets.css 인라인 필요, `.wg-NN-`(2자리) 네임스페이스 밖 누수 금지, `!important` 금지(승인된 2건 외), 위젯 영역 내 행동 `<script>` 금지.
- `mode_template_contract`: 산출은 `vt-shell|vt-frame` 래퍼 + 모드 primary_vt 마커 + 권장 wg 마커를 가져야 한다.
- 버전·`governance_count` 변경 시 `dev-plan/release-approval-v5.11.0.md` 필요. **착수 기준선은 `5.10.6` 릴리스 완료 시점의 버전·해시·governance_count**(현행 5.10.5/162가 아님 — 5.10.6에서 신규 게이트로 증가한 count 위에서 산정).
- 모드 18은 priority 18, `examples/18_*.html` 번호대 사용(현재 예제 17번 + index.html).

## Phase 상태 요약

- [ ] Phase 0. 선행 결정 게이트 & 원본 충실도 확인
- [ ] Phase 1. 위젯 CSS 작성(wg-21~28, 악성 fixture 먼저)
- [ ] Phase 2. 레이아웃 + 모드 18 + recipe 정의
- [ ] Phase 3. 무-JS 시나리오 스위처
- [ ] Phase 4. 콘텐츠 프레임워크 매핑
- [ ] Phase 5. 검증 예제(V/Q/C)
- [ ] Phase 6. 해시·패키징·거버넌스 정합

---

## Phase 0. 선행 결정 게이트 & 원본 충실도 확인

### 목표
- 후속 Phase 형태를 가르는 미결 결정을 확정하고, 원본의 시각 기준을 캡처한다.

### 구현 태스크
- [ ] **D1(확정)**: 위젯 8종 → `widgets.css` 조건부 자산, `wg-21~wg-28`. (코어해시 5종 미포함 재확인 완료)
- [ ] **D2(결정)**: 모드 primary_vt 처리 방식 — (a) 기존 vt 재사용(`process-swimlane`/`implementation-plan` 등) vs (b) 신규 vt(`stage-pipeline`, 마커 예 `sp-item`)를 **조건부 `visual-html.css`**에 추가. 코어 `visual-components.css` 변경은 회피. → 권장: (b) `visual-html.css` 신규 vt. 결정 근거 기록.
- [ ] **D3(확정 권장)**: 시나리오 토글 = radio `name="ahf-scenario"` + `:checked ~` 형제 선택자(테마 스위처 동형, no-JS). `:target` 대비 장점(초기 checked 가능·뒤로가기 비의존) 기록.
- [x] **D4(확정)**: 모드 18 신설에 따른 버전 승격 범위 → **`5.11.0`(마이너) 확정**(2026-06-21). 신규 모드+위젯 8종 = 기능 추가이므로 patch가 아닌 minor. **`5.10.6` 릴리스 이후 착수**. 신규 게이트 추가 시 `governance_count` 증가분은 5.10.6 최종 count 위에서 산정 → `release-approval-v5.11.0.md` 작성.
- [ ] 원본 인코딩 복원(UTF-8 한국어) 후 로컬 렌더 1회 → 8종 컴포넌트 시각 기준 스크린샷을 `dev-plan/` 외 임시 경로에 캡처(정본 승격 아님).
- [ ] ①구조/크롬 중복 매핑: 기존 `header.header`/`kicker`/`toc-map`/`section` 패턴과 원본의 hero/chip-nav/번호배지 대조 → 신규 필요분만 식별.

### 자체 테스트
- [ ] D1~D4 결정과 근거가 문서에 기록됨.
- [ ] `asset_order`에 `widgets.css`/`visual-html.css` 없음 재확인(코어 불변 전제 성립).
- [ ] 원본 8종 컴포넌트 렌더 스크린샷 확보(충실도 레퍼런스).

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 1. 위젯 CSS 작성(wg-21~28, 악성 fixture 먼저)

### 목표
- 시각 컴포넌트 8종을 `widgets.css`에 네임스페이스 안전하게 추가한다.

### 구현 태스크
- [ ] 기존 위젯/`visual-components.css`와 중복 점검(스텝·스코어바·카드 그리드가 이미 있으면 재사용, 신설 최소화).
- [ ] **악성 fixture 먼저**: 네임스페이스 누수(`.wg-21 .foo`)·`!important`·위젯 내 `<script>` fixture로 `widget_static_gate` 실제 실패 확인.
- [ ] `wg-21`(North Star)~`wg-28`(추천 미니카드/긴급도 핀) CSS 작성. 색·간격은 기존 토큰(`--accent`/테마 변수) 우선 사용, 8테마에서 깨지지 않게.
- [ ] 다크 패널(wg-21/wg-28)은 테마 무관 가독성 확보(고정 대비 ≥ 4.5:1).
- [ ] 화살표 커넥터(wg-22)·counter 스텝(wg-24)은 좁은 폭에서 겹침/줄바꿈 방지(이전 데모17 결함 유형 회피).

### 자체 테스트
- [ ] 악성 fixture가 게이트에서 실제로 실패함을 확인.
- [ ] 정상 마크업은 `widget_static_gate` 통과, `.wg-NN-` 네임스페이스 누수 0, 코어 `!important` 0.
- [ ] 8테마 각각에서 8종 위젯 렌더 시 대비/레이아웃 이상 없음(Playwright).
- [ ] 코어해시 불변(`visual-components.css` 등 5종 무변경).

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 2. 레이아웃 + 모드 18 + recipe 정의

### 목표
- 신규 레이아웃·모드·recipe를 기존 스키마에 맞게 정의한다.

### 구현 태스크
- [ ] `assets/layouts/strategy-os.html` 작성(`{{KICKER}}`/`{{TITLE}}`/.../`{{SOURCE_NOTE}}` placeholder, `layout-strategy-os` 클래스).
- [ ] `modes/18-strategy-os.json` 작성: `id=strategy_os`, `priority=18`, `layout_class=layout-strategy-os`, `layout_file`, `recipe`, `triggers`(전략·기술 신호·운영체계 등), `required_blocks`(전략-OS 6단계), `layout_placeholders`, `primary_vt`(D2 결정값), `vt_candidates`, `vt_markers`, `wg_candidates=[wg-21..wg-28]`, `wg_markers`, `toc_contract`, `quality_contract`(requires_body_icon/header_lens/render_audit, mode_depth_min_avg), `examples`.
- [ ] `recipes/strategy-os.prompt.md` 작성(콘텐츠 프레임워크 방법론 본문 — Phase 4와 연결).
- [ ] D2가 (b)인 경우 `visual-html.css`에 신규 vt(`stage-pipeline`) 추가.

### 자체 테스트
- [ ] 모드 JSON이 기존 모드(예: 17)와 동일 필드 스키마를 만족.
- [ ] `mode_template_contract`가 요구하는 primary_vt 마커·권장 wg 마커가 모드 정의와 정합.
- [ ] 레이아웃 placeholder ↔ 모드 `layout_placeholders` 1:1 일치.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 3. 무-JS 시나리오 스위처

### 목표
- 원본 시뮬레이터를 radio `:checked` 기반 무-JS 토글로 재현한다.

### 구현 태스크
- [ ] radio `name="ahf-scenario"`(신호 5종: model/repo/feature/regulation/competitor) + 첫 항목 `checked`.
- [ ] `:checked ~` 형제 선택자로 시나리오별 패널(시그널 히어로·파이프라인·부서 역할·스텝·스코어·결정) 토글. 공통 셸 + 시나리오별 토글 영역 구조로 중복 최소화.
- [ ] 접근성: `fieldset`/`legend`/`label`, 키보드 포커스, `aria` 라벨(테마 스위처 관례 준수).
- [ ] CSS만으로 부서 카드 dim/active 및 역할 배지 전환 표현.

### 자체 테스트
- [ ] 산출 HTML에 행동 `<script>` 0(application/ld+json만 허용).
- [ ] 5개 라디오 각각 선택 시 해당 패널만 표시됨(Playwright로 5종 전환 렌더 확인).
- [ ] 무-JS 환경(JS 비활성)에서도 첫 시나리오가 보이고 라디오 전환 동작.
- [ ] `widget_static_gate`·`validate_output` 통과.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 4. 콘텐츠 프레임워크 매핑

### 목표
- 전략-OS 6단계 방법론을 모드 계약·recipe·예제 콘텐츠로 고정한다.

### 구현 태스크
- [ ] 6단계(North Star→신호 5종→10부서 관점→실행 워크플로→결정 4종→운영 리듬 4종)를 `required_blocks`로 표준화.
- [ ] `recipes/strategy-os.prompt.md`에 각 단계의 목적·산출물·작성 규칙(부서 역할 배정, 결정 4종 정의, 리듬 4주기) 기술.
- [ ] 10부서 카드 정본 메타(KPI/핵심질문/산출물) 템플릿화 — 원본 콘텐츠를 일반화.

### 자체 테스트
- [ ] 예제가 `required_blocks` 전부 포함.
- [ ] `mode_depth_min_avg` 충족(섹션 깊이).
- [ ] recipe만 읽고도 동일 구조의 새 문서를 만들 수 있는지 1건 드라이런.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 5. 검증 예제(V/Q/C)

### 목표
- 모드 18 정본 예제를 만들고 V/Q/C를 통과시킨다.

### 구현 태스크
- [ ] `examples/18_strategy_os_*.html` 작성: 코어 CSS 5종 + 조건부 자산(widgets/visual-html) 바이트-verbatim 인라인 + `adaptive-html-final-core-css-sha256` 마커 + 8테마 스위처 + 무-JS 시나리오 + 8종 위젯 + primary_vt(vt-shell/vt-frame).
- [ ] `sources/`(profile.json/build-evidence.json/render-audit.json/screenshots) 완료 아티팩트 생성.
- [ ] `modes/18-strategy-os.json`의 `examples`에 등록, `examples/index.html` 갱신.

### 자체 테스트
- [ ] V `validate_output.py --skill-dir` OK.
- [ ] Q `quality_contract_check.py` OK.
- [ ] C `completion_check.py` 4/4(validate+quality+governance+build-evidence/render-audit).
- [ ] 1280/390 overflow_ok, 8테마 렌더·대비 감사 무결.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 6. 해시·패키징·거버넌스 정합

### 목표
- 자산 해시·패키지·버전·거버넌스 표면을 일관되게 맞춘다.

### 구현 태스크
- [ ] `css-integrity.json`(전체 자산 해시) 갱신 — widgets.css/visual-html.css 변경 반영. 코어해시는 불변임을 확인.
- [ ] 신규 게이트를 추가했다면 `tests/test_governance_gates.py` 등록 + `manifest.json` `governance_count` 증가, 악성 fixture 회귀 포함.
- [ ] D4 결정대로 버전 승격(`manifest.json` + 버전 표면 문서들) + `dev-plan/release-approval-vX.Y.Z.md` 작성.
- [ ] `.skill` 재패키징(`skill_package_content_issues` 통과: 패키지 == 체크인 디렉터리 바이트 일치).

### 자체 테스트
- [ ] 거버넌스 게이트 N/N 통과(`completion_check` 3/4 governance).
- [ ] `completion_check` 4/4 유지.
- [ ] 패키지 content gate·버전 surface 정합 게이트 통과.
- [ ] 모든 예제 재인라인 일관성(기존 18 예제 + 신규) 확인.

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 워크스트림 종료(또는 사용자 커밋 요청 대기)

## 잔여 리스크 / 후속 과제

- 무-JS 시나리오 토글이 5벌 패널 복제로 HTML이 커질 수 있음 → 공통 셸 + 토글 영역 분리로 완화, 페이지 용량 모니터링.
- 신규 vt를 `visual-html.css`에 둘지 코어로 승격할지(D2)가 향후 다른 모드와의 일관성에 영향 → 결정 근거를 모드 문서에 남길 것.
- 10부서 콘텐츠 일반화 수준(범용성 vs 구체성) 균형 — recipe 드라이런으로 검증.
