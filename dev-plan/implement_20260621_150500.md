# implement_20260621_150500.md

작성 일시: `2026-06-21 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

> ### ⚑ 릴리스 방식 갱신 (2026-06-21) — 버전 업 없이 5.10.6 병합
> 사용자 결정: business_plan(mode 18)을 **별도 `5.11.0` 발행 없이 현행 `5.10.6` 릴리스에 병합**한다. 가능 근거 = **코어 CSS 무변경**(implementation-plan vt + 기존 wg-16/11/13/18/14 재사용, layout 전용 CSS를 layouts.css에 넣지 않음) → **코어 해시 `a64604d0` 불변** → 기존 18 예제 재인라인 불필요. 따라서 **추가(additive)만**: 모드 JSON·layout 템플릿·recipe·references·4 게이트·신규 예제 1개 + governance_count·mode count(17→18)·examples count 증가 + `release-approval-v5.10.6.md`에 mode 18 흡수 + `.skill` 재패키징. **버전 문자열 surface는 전부 5.10.6 유지**(bump 없음). 커밋은 **브랜치 없이 main 직접**(사용자 승인 후).

## 개발 목적

`adaptive-html-final`에 **18번째 모드 `business_plan_html`을 신규 추가**하고, **버전 업 없이 현행 `5.10.6` 릴리스에 병합**한다(위 배너). 사용자 결정(2026-06-21): mode 18 슬롯 = `business_plan_html`(strategy-os의 18 주장 폐기 — strategy-os는 소스 미커밋으로 slot 21+ 보류).

소스는 `orginal_skill/bizplan/`(리포 커밋됨, 2.5M)이며, 사용자 프로토타입이 [output/2026-06-21/new-mode-pretest/business-plan-html/index.html](../output/2026-06-21/new-mode-pretest/business-plan-html/index.html)에 존재한다(QA 통과). 기존 17모드가 강제할 수 없는 **증거태그 문법·단일 숫자 레지스트리/출처원장·document_type 게이트·4-평가자 자기검토 스코어카드**를 정본 계약으로 고정하는 것이 목적이다.

## 개발 범위

- **모드 등록**: [modes/18-business-plan.json](../skills/adaptive-html-final/modes/) — 스켈레톤([docs/orginal-skill-mode-entry-skeletons-20260621.json](../docs/orginal-skill-mode-entry-skeletons-20260621.json)) 기반. priority 18, layout_class `layout-business-plan`, primary_vt `implementation-plan`, **wg-16/11/13/18/14 재사용**(신규 위젯 없음), required_blocks 13종, custom_contracts 4종.
- **레이아웃**: `assets/layouts/business-plan-report.html` — 17 placeholder(`{{GENERATED_ROW}}`…`{{SUBMISSION_CHECKLIST}}`).
- **recipe**: `recipes/business-plan.prompt.md` — 사업계획 6단계 방법론(공고분석→인터뷰→리서치→business-core→숫자/재무→평가자 시뮬).
- **references 포팅**: `orginal_skill/bizplan/references/`(evidence-tagging·business-core·source-ledger 등)에서 AHF 정본 계약으로 일반화한 reference 1종(예: `references/business-plan-system.md`).
- **4 custom gates**(validate_output.py, 모드 self-gate 방식 = `github_analysis_visual_contract_gate` 패턴): `business_plan_evidence_tag_gate`·`business_plan_number_consistency_gate`·`business_plan_source_gate`·`business_plan_status_gate`. 악성 fixture 먼저 FAIL 입증 후 통과.
- **레퍼런스 예제**: `examples/18_business_plan_grantproof_core.html` — 코어 CSS(현행 해시 `a64604d0`) 인라인 + 8테마 + 무-JS + required_blocks 전부 + sources/(profile·build-evidence·render-audit·screenshots).
- **거버넌스·표면(버전 업 없음)**: governance_count 176→+(신규 게이트), **버전 문자열 5.10.6 유지**(bump 없음), `release-approval-v5.10.6.md`에 **mode 18 흡수**(신규 v5.11.0 문서 미생성), mode count 17→18 + examples count 18→19(README/manifest/SKILL/Guide/AGENTS 표면), `.skill` 재패키징. CHANGELOG는 신규 버전 섹션이 아니라 **v5.10.6 엔트리에 mode 18 줄 추가**.

## 제외 범위

- **신규 위젯/vt 추가 없음** — primary_vt=implementation-plan + wg-16/11/13/18/14 기존 자산 재사용. **코어 CSS 5종 무변경 → 코어 해시 `a64604d0` 불변**(examples 재인라인 rebase 불필요; 신규 example만 현행 해시 인라인).
- **bizplan 멀티페이지 패키지·.xlsx evidence-ledger·외부 스크립트**: AHF 무-JS·단일-HTML 불변에 맞춰 **정적 in-HTML 테이블로 다운컨버트**(D3). 원본 빌더(`scripts/build_bizplan_*.py`)는 output 생성용이며 스킬 입력 아님.
- **strategy-os(mode 18 주장 폐기)·storm_research(5.12.0)·social_trend(5.13.0)·operator_manual(manual_analysis 강화)** — 본 차수 제외.
- output/ 산출물 정본 승격(독립 결과물). 프로토타입은 시각 충실도 레퍼런스로만 1회 참조.

## 참조 문서

- [docs/orginal-skill-new-mode-entry-extraction-20260621.md](../docs/orginal-skill-new-mode-entry-extraction-20260621.md) §4.1 — 모드 entry 스펙
- [docs/orginal-skill-mode-entry-skeletons-20260621.json](../docs/orginal-skill-mode-entry-skeletons-20260621.json) — 18-business-plan 스켈레톤
- [dev-plan/implement_20260621_130535.md](implement_20260621_130535.md) — orginal_skill 통합 상위 계획(P0=business_plan)
- [dev-plan/implement_20260618_221706.md](implement_20260618_221706.md) — 직전 5.10.6 릴리스(add-mode 서피스·게이트·재패키징 절차 참조)
- [dev-plan/release-approval-v5.10.6.md](release-approval-v5.10.6.md) — 승인 문서 양식
- 소스: `orginal_skill/bizplan/`(references·templates/business-core.yaml·dist/bizplan-sme-global-rnd) · 프로토타입 `output/2026-06-21/new-mode-pretest/business-plan-html/`
- 현행 상태(단일 출처): [skills/adaptive-html-final/manifest.json](../skills/adaptive-html-final/manifest.json) — **v5.10.6 / governance 176 / 17 modes / 코어 해시 a64604d0**

## 공통 진행 규칙

- 각 Phase는 앞선 Phase 자체 테스트 완료 후 시작. 이슈는 해당 Phase에서 수정·기록.
- 체크박스를 실제 진행과 일치시킨다. 문서 밖 범위 확장 금지.
- 신규 게이트는 **악성 fixture로 먼저 FAIL 입증** 후 통과로 전환 + `governance_count` 동반 증가.
- 버전·governance_count·mode count는 `manifest.json` 단일 출처와 동기화.
- **코드 변경·커밋은 사용자 명시 요청 시에만**. output은 독립 결과물(검증 입력 아님).
- ⚠️ **IDE revert 주의**([[ide-reverts-skill-files-during-edits]]): 스킬 파일 다수 편집 시 외부 IDE/린터가 git HEAD로 revert할 수 있음 — 편집 후·커밋 전 on-disk 재확인 필수.

## 문서 기반 제약 사항

- 무-JS 불변(JSON-LD만). 신규 모드 산출에 `<script>` 금지.
- `mode_template_contract`: 산출은 `vt-shell|vt-frame` + primary_vt(`implementation-plan` → `plan-grid`/`milestone` 마커) + 권장 wg 마커.
- 신규 모드 추가는 통상 minor지만, **사용자 결정으로 버전 업 없이 5.10.6에 병합**. `version_release_approval_issues` 게이트는 manifest(5.10.6) ≠ HEAD(5.10.5)에서 `release-approval-v5.10.6.md` 존재로 이미 충족 — mode 18 범위만 그 문서에 흡수.
- 코어 해시 자산 5종 불변 전제(신규 위젯/vt 없음) — `widgets.css`/`visual-html.css`도 미변경이면 `css-integrity.json` 조건부 해시도 불변. 신규 example은 현행 `a64604d0`를 그대로 인라인.
- mode count 17→18: `check_mode_registry_sync.py`·README/매니페스트 모드 수 표면 동기화.

## 선행 결정 게이트 (착수 전 확정)

| 결정 | 선택지 | 권장 |
|---|---|---|
| **D1. `number_consistency_gate` 검출 방식** | (a) 구조화 `number_registry` 블록 ↔ 본문 숫자 토큰 diff vs (b) 통화/% 정규식 스캔 | **(a)** — `number_registry`를 정본 블록(레이블·값·단위·출처ref)으로 선언하고 본문/표 발생과 대조. (b)는 너무 약하거나 brittle. |
| **D2. `evidence_tag_gate` 결정적 트리거** | "모든 핵심 주장"(비결정적) vs **명시 범위** | `number_registry`·`market_finance_model` 블록 내 숫자/주장 토큰은 `[사실]/[추정]/[가정]/[목표]/[확인필요]` 중 하나를 **반드시** 앞/뒤에 동반. pass/fail 결정 가능한 범위로 한정. |
| **D3. 출처원장 정적 재설계** | xlsx ledger 유지(불가) vs **in-HTML 정적 테이블** | in-HTML `source-ledger` 테이블(publisher/date/as-of/access-date/URL 컬럼) + 컬럼 존재 검사 게이트(`business_plan_source_gate`). |
| **D4. 평가자 스코어카드 성격** | 외부 검증(날조 위험) vs **자기검토 시뮬** | admin/tech/biz/skeptic 4-평가자 **자기검토 시뮬레이션**임을 recipe·예제에 명시(bizplan 규칙#1 날조 금지 준수). `business_plan_status_gate`는 점수 근거 라벨 강제. |
| **D5. 한국어 태그 어휘** | 확정 | `[사실]·[추정]·[가정]·[목표]·[확인필요]` 5종 고정. 영문 변형은 1차 범위 외. |
| **D6. 버전** | 확정 | **버전 업 없음 — 5.10.6에 병합**(사용자 결정 2026-06-21). 코어 해시 a64604d0 불변·신규 v5.11.0 미발행. |

## Phase 상태 요약

- [ ] Phase 0. 결정 게이트 D1~D6 확정 + bizplan 소스·프로토타입 충실도 확인 + baseline
- [ ] Phase 1. references 포팅 + recipe + 13 required_blocks 계약 정의
- [ ] Phase 2. 레이아웃 `business-plan-report.html` + `modes/18-business-plan.json`
- [ ] Phase 3. 4 custom gates(악성 fixture 먼저) + validate() dispatch + test 등록 + governance++
- [ ] Phase 4. 레퍼런스 예제 `examples/18_business_plan_*.html`(V/Q/C 통과)
- [ ] Phase 5. 표면 정합(버전 5.10.6 유지) + mode count 18 + release-approval-v5.10.6 흡수 + .skill 재패키징

---

## Phase 0. 결정 게이트 & 소스 충실도

### 구현 태스크
- [ ] D1~D6 결정·근거를 본 문서에 확정 기록.
- [ ] `orginal_skill/bizplan/references/`(evidence-tagging·07-business-core·source ledger)·`templates/business-core.yaml`·`dist/bizplan-sme-global-rnd` 정독 → AHF 정본으로 일반화할 계약 추출.
- [ ] 프로토타입 `output/2026-06-21/new-mode-pretest/business-plan-html/index.html` + `qa-report.md` 검토 → 시각/구조 기준 + 알려진 결함 목록화.
- [ ] 13 required_blocks ↔ 17 layout_placeholders 1:1 매핑표 확정.

### 자체 테스트
- [ ] D1~D6 + 근거 문서화. required_blocks↔placeholder 매핑 완전성 확인.
- [ ] 현행 baseline 실측(5.10.6 / governance 176 / 17 modes / a64604d0) 기록.

### 완료 조건
- [ ] 구현/자체 테스트 완료 · 다음 Phase 진행 가능

## Phase 1. references + recipe + required_blocks 계약

### 구현 태스크
- [ ] `references/business-plan-system.md` 작성 — 6단계 방법론·증거태그 어휘(D5)·number_registry/source-ledger 계약(D1/D3)·평가자 시뮬 규칙(D4).
- [ ] `recipes/business-plan.prompt.md` 작성 — document_type(govt/R&D/pitch/proposal/internal) 분기 + 13 블록 작성 규칙.
- [ ] 13 required_blocks 표준화(generated_row…source_note) + 각 블록의 정본 컴포넌트(implementation-plan vt·wg-16/11/13/18/14·source-note 등) 지정.

### 자체 테스트
- [ ] recipe만 읽고 동일 구조 문서를 만들 수 있는지 1건 드라이런.
- [ ] §0.6 매핑·MODE_TEMPLATE_CONTRACTS와 정합(신규 모드 vt/wg 계약).

### 완료 조건
- [ ] 구현/자체 테스트 완료 · 다음 Phase 진행 가능

## Phase 2. 레이아웃 + 모드 JSON

### 구현 태스크
- [ ] `assets/layouts/business-plan-report.html`(`layout-business-plan` 클래스 + 17 placeholder). 코어/조건부 CSS 변경 없이 기존 토큰·컴포넌트만 사용.
- [ ] `modes/18-business-plan.json` 작성(스켈레톤 기반): id·priority 18·triggers 10·required_blocks 13·layout_placeholders 17·primary_vt·vt_candidates·vt_markers·wg_candidates(16/11/13/18/14)·wg_markers·toc_contract(structural_h2_gte_4)·quality_contract(body_icon/header_lens/render_audit, mode_depth_min_avg 400)·examples·custom_contracts 4종.

### 자체 테스트
- [ ] 모드 JSON이 기존 모드(17) 동일 필드 스키마 만족. `check_mode_registry_sync.py` 통과.
- [ ] layout placeholder ↔ 모드 layout_placeholders 1:1, `mode_template_contract` 마커 정합.

### 완료 조건
- [ ] 구현/자체 테스트 완료 · 다음 Phase 진행 가능

## Phase 3. 4 custom gates (악성 fixture 먼저)

### 구현 태스크
- [ ] `business_plan_evidence_tag_gate(text, style)` — D2 범위 내 숫자/주장 토큰에 5종 태그 부재 시 실패(layout-business-plan self-gate).
- [ ] `business_plan_number_consistency_gate(text)` — D1: `number_registry` 값 ↔ 본문/표 발생 불일치 시 실패.
- [ ] `business_plan_source_gate(text)` — D3: `source-ledger` 테이블 필수 컬럼(publisher/date/as-of/access/URL) 부재 시 실패.
- [ ] `business_plan_status_gate(text)` — D4: 평가자 스코어카드 점수에 근거 라벨/자기검토 표기 부재 시 실패.
- [ ] validate() per-page dispatch에 4종 추가(github_analysis_visual_contract_gate 패턴). test_governance_gates.py에 각 catch/pass 등록 + `governance_count` 증가 + manifest 동기화.

### 자체 테스트
- [ ] 악성 fixture 4종이 게이트에서 실패. 정상 마크업 통과.
- [ ] 기존 examples 18종(17 reference) 회귀 0 — 신규 게이트는 layout-business-plan에만 적용(self-gate)되어 타 모드 무영향.
- [ ] governance N/N(신규 count = manifest).

### 완료 조건
- [ ] 구현/자체 테스트 완료 · 다음 Phase 진행 가능

## Phase 4. 레퍼런스 예제 (V/Q/C)

### 구현 태스크
- [ ] `examples/18_business_plan_grantproof_core.html` — 코어 CSS 5종(현행 해시 `a64604d0`) 바이트-verbatim 인라인 + `core-css-sha256` 마커 + 8테마 + 무-JS + 13 required_blocks + 증거태그/number_registry/source-ledger/스코어카드 실콘텐츠. 프로토타입을 정본 자산 기준으로 재작성.
- [ ] `sources/`(profile·build-evidence·render-audit·screenshots) 완료 아티팩트. `modes/18-business-plan.json` examples 등록 + `examples/index.html` 갱신(17→18 카드).

### 자체 테스트
- [ ] V `validate_output.py examples --skill-dir` OK(신규 게이트 4종 + inline hash·verbatim·snapshot 포함, 코어 해시 a64604d0 불변 확인).
- [ ] Q `quality_contract_check.py` OK · C `completion_check.py` 4/4(render-audit·build-evidence 포함).
- [ ] 1280/390 overflow_ok, 8테마 대비 감사 무결, mode_depth_min_avg 400 충족.

### 완료 조건
- [ ] 구현/자체 테스트 완료 · 다음 Phase 진행 가능

## Phase 5. 표면 정합(버전 유지) + mode 18 흡수 + 패키징

### 구현 태스크
- [ ] `manifest.json`: **version 5.10.6 유지(bump 없음)**, modes 17→18 등록, examples count 17→18, `governance_count` 갱신, **changes[]/releases[] v5.10.6 엔트리에 mode 18 줄 추가**, `updated`.
- [ ] 모드수·governance 표면 일괄(버전 문자열 불변): SKILL.md·**CHANGELOG v5.10.6 엔트리에 mode 18 추가**·README(repo+skill, 모드 17→18·governance·배지)·AGENTS·Guide·visual-html-system·examples footer.
- [ ] `dev-plan/release-approval-v5.10.6.md`에 **mode 18(business_plan) 범위 흡수**(신규 v5.11.0 문서 미생성).
- [ ] `examples/sources/adaptive-html-final-manifest.json` 재동기화 + `.skill` 재패키징(byte-match). **코어 해시 불변이므로 기존 17 example 재인라인 불필요** — 신규 18 example만 추가.

### 자체 테스트
- [ ] `test_governance_gates.py` = 신규 count, manifest 일치. `validate_output examples --skill-dir` 0 issue. quality·completion OK.
- [ ] 버전 표면 게이트(`output_visible_version_stale`·`readme_*`·`skill_md_version_mismatch`·`manifest_version_*`·`skill_package_*`·`version_release_approval`) 통과. mode count 18 표면 정합.
- [ ] 커밋 직전 revert-가드 재확인(IDE revert 주의) + 신규 untracked 파일(modes/layout/recipe/references/example/sources) git-add 목록 정리.

### 완료 조건
- [ ] 구현/자체 테스트 완료 · 전체 워크스트림 완료(또는 사용자 커밋 요청 대기)

## 잔여 리스크 / 후속 과제

- **게이트 결정성**: evidence_tag/number_consistency의 pass/fail 경계가 모호하면 오탐/누락 → D1/D2를 "구조화 블록 한정"으로 좁혀 결정적 검사 보장(악성 fixture로 검증).
- **스코어카드 날조 위험**: 4-평가자 점수가 모델 창작이면 bizplan 규칙#1 위반 → "자기검토 시뮬" 명시 + 점수 근거 라벨 강제(D4).
- **출처원장 다운컨버트**: xlsx→정적 HTML 테이블로 줄이며 정보 손실 가능 → 필수 컬럼 계약으로 최소 보장.
- **mode 라우팅**: business_plan triggers와 expert_html 충돌 최소(중복 얕음)이나 triggers 어휘를 사업계획 특화로 분명히.
- **후속 모드**: storm_research(5.12.0)·social_trend(5.13.0)는 본 차수 종료 후 별도 계획. strategy-os는 `company_ai_strategy_dashboard.html` 커밋 시 21+ 슬롯으로 재검토.
