{
  // ───────────────────────────────────────────────────────────────
  // 목표: 탐색기에 git 수정 표시(금색)만 남기고, 빨강/노랑 "문제(Problems)" 표시는 모두 제거.
  // 방법: explorer 데코레이션은 끄지 않고(=git 유지), 문제 마커를 만드는 내장 진단기를 전부 차단.
  // 적용 후 1회 "Developer: Reload Window"로 이미 떠 있는 캐시 마커를 비우면 재발 없음.
  // ───────────────────────────────────────────────────────────────

  // ★ Problems(린트) 데코레이션만 끔 → 빨강 에러·노랑 경고·"9+" 배지 제거. git 색과는 별개.
  "problems.decorations.enabled": false,

  // ★ git 변경 색은 살림(modified=금색, untracked=초록). git만 색칠하고 Problems는 위에서 껐으므로
  //    린트 빨강/노랑은 안 나온다. git 기본 '삭제(D)' 색만 빨강이라, 아래 colorCustomizations로 금색 처리.
  "explorer.decorations.colors": true,
  "workbench.colorCustomizations": {
    // git 삭제/충돌은 기본 빨강 → 금색으로 바꿔 트리에 빨강이 전혀 안 뜨게.
    "gitDecoration.deletedResourceForeground": "#d99a38",
    "gitDecoration.conflictingResourceForeground": "#d99a38",
    "gitDecoration.stageDeletedResourceForeground": "#d99a38",
    // 혹시 남는 Problems 잔상 색도 중립화(이중 안전장치).
    "list.errorForeground": "#cfcfcf",
    "list.warningForeground": "#cfcfcf"
  },

  // HTML / CSS / LESS / SCSS 내장 검증 끄기
  "html.validate.enable": false,
  "html.validate.styles": false,
  "html.validate.scripts": false,
  "css.validate": false,
  "less.validate": false,
  "scss.validate": false,

  // JS / TS / JSON 내장 검증 끄기
  "javascript.validate.enable": false,
  "typescript.validate.enable": false,
  "json.validate.enable": false,

  // Python(Pylance) 진단 끄기: 워크스페이스 전체 분석 금지 + 타입검사 off + 전 파일 무시
  "python.analysis.diagnosticMode": "openFilesOnly",
  "python.analysis.typeCheckingMode": "off",
  "python.analysis.ignore": ["**"],
  "python.linting.enabled": false,

  // (참고) 아래 확장들은 현재 미설치라 무효지만, 설치 대비 제외 규칙 유지
  "htmlhint.exclude": { "**/output/**/*": true, "output/**/*": true },
  "markdownlint.config": { "default": false },
  "markdownlint.run": "off",
  "sonarlint.analyzerProperties": {
    "sonar.exclusions": "**/output/**,**/output/**/*,**/skills/**"
  },
  "eslint.enable": false,
  "eslint.options": { "ignorePattern": ["**/output/**", "**/skills/**"] }
}
